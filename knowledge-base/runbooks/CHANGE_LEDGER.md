# Production change ledger (append-only)

> **Scope rescoped 2026-08-18** (founder-approved — [TEAM_PRACTICES §6](../governance/TEAM_PRACTICES.md)):
> rows are required for hand actions against prod DB/env, incidents + forward fixes, §9-trigger
> deploys, and special-rollback changes (contract migrations, destructive data, dependency
> majors). Ordinary green PR merges are recorded by the PR itself plus CI's `verify-deploy`
> commit-field check and no longer get per-merge rows — which is why this ledger is quiet
> between 2026-07-19 and this note: doctrine has been brought to match the practice that the
> `verify-deploy` era made safe. Rows below remain immutable history.

> Split out of [CICD.md](./CICD.md) on 2026-08-06. The ledger had grown to ~60% of that file and
> buried the operational runbook underneath it — you read the runbook to *do* a deploy and the
> ledger to *audit* one, and they were never needed together. **Nothing was rewritten:** every row
> below is verbatim, and the reading notes that governed them still apply.

Every push to `main` (it deploys) and every action against the production database or its
env vars gets a row here **in the same session** — newest first
([TEAM_PRACTICES](../governance/TEAM_PRACTICES.md) §6). Never rewrite or delete rows; corrections get a
new row. Each row: what shipped · prod DB/env actions (and how) · validation evidence ·
rollback pointer. Two reading notes for rows written before 2026-08-06: they describe the
Vercel era (deployment IDs, preview URLs, `vercel.json`), and any cross-reference to the
section *"How the Vercel deploy works"* now resolves to **"How the Railway deploy works"**
above — the rules it carries (pnpm-only lockfile, `pnpm.overrides` floor) survived the
platform change unaltered. From 2026-08-06 on, the validation column's live-prod evidence is the
**`commit` comparison** described above — a 200 from `/api/health` no longer counts on its own.
Rows below that date describe the Vercel era and are left exactly as written.

| Date | Change | Prod DB / env | Validation | Rollback |
|---|---|---|---|---|
| 2026-09-10 | **PR #782 — strengthened production proof for Homi and private evidence storage.** Homi's canary now runs a fixed grounded status turn through the production prompt/context builder, real status-tool schema and outbound compliance lint. It uses synthetic server truth, executes no tool and reads no borrower file. A new two-phase private-storage check requires a CRC-protected synthetic marker written by one Railway deployment to be read and deleted by a different deployment on the same commit within one hour; same-process, changed-commit, stale, malformed, missing-identity and missing-object cases fail closed. The existing borrower-data-free sweep still checks the real synthetic pay-statement extraction path with page evidence, mixed-income analysis and deterministic underwriting. A proposed lower-effort document setting was rejected before release when W-2 and bank-statement trials lost required source evidence, so production inference settings did not change. | No database, schema, environment variable, credential, dependency or borrower-data change. Main workflow [`34420007528`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34420007528) deployed commit `f84adce7ab153ca0c1a7426c9744bae0ceaa4aa9`. After the storage seed, an explicit same-build Railway redeploy created deployment `599e3605-8694-4d64-a21d-796b51850dc5`; it reached `SUCCESS`. The private synthetic marker contained only deployment identity, public commit, timestamp and nonce and was deleted by successful verification. | Required gate run [`34419613525`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34419613525) and the complete local preflight passed. Exact health at [`/api/health?verify=f84adce7`](https://www.homiquity.com/api/health?verify=f84adce7ab153ca0c1a7426c9744bae0ceaa4aa9) returned the same full commit. Before restart, sweep [`34420267104`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34420267104) passed grounded Homi, pay-statement extraction, storage, financial analysis and underwriting in 4.104 s, 10.112 s, 0.591 s, 0.006 s and 0.787 s. Seed run [`34420317049`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34420317049) wrote the marker. Verify run [`34420451894`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34420451894) read and deleted it from the new deployment after 109.370 s; the read/validate/delete operation took 0.382 s. Post-restart sweep [`34420484805`](https://github.com/barakatammre84/homiquity-mortgage-broker/actions/runs/34420484805) passed all five checks on the same commit in 4.079 s, 9.645 s, 0.483 s, 0.004 s and 0.552 s. This proves generic private-object survival across the process replacement; it does not prove in-flight extraction-job recovery, production 100-page capacity, model accuracy or lender acceptance. | [ROLLBACK.md](./ROLLBACK.md) §1 — restore the preceding Railway deployment or `git revert f84adce7ab153ca0c1a7426c9744bae0ceaa4aa9`; no schema or row rollback is required. Retained canary rows remain append-only operational evidence. The restart marker was already deleted, so no object cleanup remains. |
| 2026-09-09 | **Anthropic production credential incident repaired; PR #781 expands the twice-daily core proof.** The first diagnostic production sweep showed that Homi and PDF vision extraction both failed provider authentication while private Google Cloud Storage remained healthy. The revoked credential was replaced, and this PR adds borrower-data-free mixed-income and deterministic underwriting checks to the same scheduled sweep: salary + side-business Schedule C + positive and negative rental treatment must reproduce the same financial fingerprint, while a fixed synthetic conventional file must reproduce the same decision and policy fingerprint from the live lookup tables. The document check now also exercises Homiquity's actual pay-stub prompt, schema validation, exact field extraction, page evidence, document classification and lineage hash. No borrower data, provider response body, financial output or policy value is retained in the canary ledger. | Railway project `Homiquity`, service `Homiquity`: replaced `ANTHROPIC_API_KEY` through stdin from an existing valid managed credential; no value was printed or committed. Railway deployment `bfd797e1-6105-4e73-b0b3-d98e33dc429a` completed `SUCCESS` on commit `8c57107d2575078703d2b79114f2db62410016db`. No database, schema, dependency or other environment variable changed. | Before repair, production workflow run `34415702042` returned HTTP 503 with two redacted `authentication` failures (Homi 430 ms; document extraction 114 ms) and a successful private-storage round trip (550 ms), all tied to commit `8c57107d2575078703d2b79114f2db62410016db`. A direct provider credential probe changed from 401 to 200 after replacement. After Railway redeployed, workflow run `34415907770` returned HTTP 200: Homi 1,161 ms, synthetic-PDF vision 1,328 ms and private write/read/delete 623 ms, all successful on that exact commit; an independent `https://www.homiquity.com/api/health?verify=8c57107d2575078703d2b79114f2db62410016db` probe reported the same commit. PR #781's real local pay-stub extraction canary passed in 9,676 ms, its focused suites passed 80 unit and 5 integration tests, and the full local preflight passed every repository guard, complete unit/client collection, high-severity production dependency audit, production build and boot, bundle ratchet, and clean-database integration lane. | Do not restore the revoked credential. If the replacement is later rejected, install a newly issued Anthropic key; removing the key is the safe degradation path (labeled offline Homi and manual document review). Revert PR #781's merge commit to return the scheduled proof to three capabilities; canary rows are append-only operational evidence and remain in place. |
| 2026-09-08 | **PR #766 — truthful mixed-income application and complete borrower handoff.** The fast application now keeps one reported household total while retaining every W-2, business/1099, rental and other source as non-additive detail; captures business structure, ownership, history, occupancy, unit count and subject rent; carries those facts into URLA, the borrower graph, source-specific document work, pricing and letters; and waits for intake finalization so the results-page and sidebar action counts agree. Complex-income DTI and pricing stay explicitly preliminary until accepted evidence and licensed review establish the usable figures. | Main workflow run `34189180398` completed `migrate-prod` and applied additive migration `0067_capture_subject_property_intent`: nullable `occupancy_type`, `number_of_units` and `subject_monthly_rental_income` columns on `loan_applications`. No destructive data action, environment-variable, provider or dependency change. | PR gate run `34188900100` passed all required checks. The full local preflight also passed type/schema/migration and repository guards, unit/client tests, dependency audit, production build, bundle ratchet, production-mode boot and the integration lane. Codex Security scan `c8000ce4-789c-4bed-88f0-855b362e5689` completed with 38/38 changed review items closed and no reportable findings; its one post-snapshot partial-draft validation patch was separately reviewed and tested. CI `verify-deploy` passed, and an independent `https://www.homiquity.com/api/health` probe reported merge commit `dcd4ff03ec5acce2d94717cf55b3e663d7b94118` with SendGrid configured; `/api/articles` and `/sitemap.xml` both returned 200. | [ROLLBACK.md](./ROLLBACK.md) §1 — restore the preceding Railway deployment or `git revert dcd4ff03ec5acce2d94717cf55b3e663d7b94118`. Migration 0067 is expand-only; leave its nullable columns in place after a code rollback so no stored occupancy/rent facts are destroyed. |
| 2026-09-06 | **PR #755 — Core overlap retirement and review-write resilience.** Removed the obsolete document-request compatibility endpoint and the duplicate asynchronous condition-revert writer so borrower document correction retains one transactional source of truth. Legacy requests without a stored status remain actionable; borrower action-item and task checks now wait independently for their asynchronous projections. File checkpoint and financial workpaper, review and memo writes now replay the whole transaction, at most three attempts, only after PostgreSQL serialization or deadlock aborts; role gates, active deal-team scope, stale-review protection, unique versions and append-only review history remain enforced. | Main workflow run `34043571322` completed `migrate-prod`; this release contained no migration, schema change, production data write, environment-variable change or external-provider change. | PR gate run `34043398776` passed; the full local preflight passed type/schema/guard checks, unit and client tests, production dependency audit, production build, bundle ratchet, production-mode self-host and all 23 integration files. A deterministic concurrency test reproduced the staff-presence serialization collision and proved simultaneous saves create one new checkpoint without a 500. Codex Security scan `49541bfd-2fc1-4a7b-a2f2-bd53d95f0717` completed with full coverage and no findings on the exact release tree. CI `verify-deploy` passed, and an independent `https://www.homiquity.com/api/health` probe reported merge commit `478ee5ec850a487adcc69da9f54fba55bfa73828` with SendGrid configured. | [ROLLBACK.md](./ROLLBACK.md) §1 — restore the preceding Railway deployment or `git revert 478ee5ec850a487adcc69da9f54fba55bfa73828`; no schema or row rollback is required, and existing immutable request, document, checkpoint, workpaper, memo and audit history remains readable. |
| 2026-09-05 | **PR #753 — Core borrower correction loop inside the existing Homiquity application.** One application-scoped secure request now carries the borrower through atomic upload, exact rejection, immutable same-lineage replacement and human acceptance; concurrent equivalent requests deduplicate, current accepted evidence drives downstream tax, DSCR, reconciliation, graph, prediction and risk-brief signals, and access loss redacts message content. The §9 security review found and closed six application-scope and stale-evidence gaps before the final exact-tree pass. | `migrate-prod` completed on main workflow run `33994803285`; this release contained no migration, schema change, production data write, environment-variable change or external-provider change. | PR gate passed; local release lanes passed 4,507 tests with 3 intentional skips; Codex Security scan `e6099cea-9d0e-40df-ac32-8194ec9fa7ca` reported no remaining findings on the exact release tree; CI `verify-deploy` passed and an independent `https://www.homiquity.com/api/health` probe reported commit `b55160852e8a62aaa5d933406b1f55eda323d79b`. | [ROLLBACK.md](./ROLLBACK.md) §1 — restore the previous Railway deployment or `git revert b55160852e8a62aaa5d933406b1f55eda323d79b`; no schema or row rollback is required, and existing immutable request, document and review history remains readable. |
| 2026-08-06 | **Platform cutover to Railway — the docs made true, plus a deploy-verification loop; this ledger row rides in the same PR.** Hosting moved off Vercel over the preceding days: `railway.json` as config-as-code (#411); the platform features reimplemented in-process — bot prerender `server/prerender.ts`, Express private-beta gate `server/middleware/betaGate.ts`, and the GitHub Actions cron scheduler `.github/workflows/cron-jobs.yml` replacing the platform cron block (#412); the `gate` taught to build **and boot** the self-host artifact against a real Postgres (#409); `engines.node` unbroken (#422 — `"24.x"` is npm range syntax `mise` cannot resolve, which had failed **nine consecutive deploys**); canonical host pinned to `www` (#423); and the Vercel surface (`vercel.json`, `api/index.ts`, root `middleware.ts`) deleted along with the Vercel project itself (#424 — the API now 404s it; there is no account surface left). Riding in **this** PR: `/api/health` reports `RAILWAY_GIT_COMMIT_SHA` as `commit`, a new `verify-deploy` CI job polls it after every push to `main` and fails when prod is not serving that SHA, and this runbook's Vercel prose (deploy mechanics, rollback, post-deploy check, beta gate, env vars, scheduler) is rewritten to Railway. **The lesson that motivated all of it: a failed Railway deploy leaves the PREVIOUS container serving, so the site stays up and every check stays green while prod goes stale — on 2026-08-06 prod sat ~8 commits behind, including go-live prep. "SUCCESS" in a dashboard and a 200 from `/api/health` are not evidence a merge shipped; only the `commit` field is.** | Production env vars now live as **Railway service variables** (Railway → project *Homiquity* → service *Homiquity* → Variables), not a Vercel project: `CRON_SECRET` must match the GitHub repository secret of the same name, and `VITE_*` vars are **build-time** (a change needs a redeploy, not a restart). Same incident window, recorded because it is a prod-env action class: the service's `DATABASE_URL` was found pointing at a **stale Neon branch** (28 of 53 migrations, no writes since 07-15) — `/api/health` stayed **200** because its `SELECT 1` succeeded *against the wrong database*, while `/api/articles` and `/sitemap.xml` 500'd. No `migrations/` or `shared/schema/` change in this PR; Neon is still the database and the `migrate-prod` job is unchanged. | `https://www.homiquity.com/api/health` → 200 `{"status":"ok"}`, and the data-reading probes `/api/articles` and `/sitemap.xml` → **200** (those are the ones that expose a wrong-branch `DATABASE_URL`; health alone does not). At the time this row was written prod was still serving a build predating the `commit` field, so the SHA comparison is proven by the `verify-deploy` job on this merge rather than retroactively. | [ROLLBACK.md](./ROLLBACK.md) §1 — now **Railway → project Homiquity → service Homiquity → Deployments → the last good one → Rollback** (restores that image *and* the variables it deployed with, no rebuild; image retention is limited — 72h on Hobby). `railway redeploy` rebuilds the latest (broken) commit and `railway restart` reuses the current image — neither is a rollback. The docs/CI half of this PR is `git revert`-safe; the platform move is not (there is no Vercel project to return to). |
| 2026-08-05 | **PR #385 — financial re-audit F-17/F-18/F-19: the non-QM dead band resolved, and pricing policy made admin-editable; this ledger row rides in the same PR.** **F-17 (High):** the platform's FIXED $2,100 of fees met a QM points-and-fees cap that is 5% in one tier, a flat $4,139 in the next and 3% above — producing a **non-QM dead band** ($101,951–$216,299 at the default 200 bps plan; $90,623–$288,399 at 225 bps) with originable loans on BOTH sides, sitting over the DPA/first-time-buyer segment, and non-monotonic (a $100k borrower originable, the same borrower at $150k not, at $400k again). An over-cap file hard-blocks at the AUS gate AND lender-package assembly, so it was fully costed and yielded zero revenue. Resolved by removing the FIXEDNESS, not by re-pricing: platform fees are now a **ceiling** — the standard schedule when it fits, the reducible part trimmed (rounding DOWN) when it does not, via a binary search over the existing tier tables so they are never duplicated. **Only what is ours is reducible** — the tax service fee is a vendor pass-through, `reducible: false`. Swept at $1 granularity $20k–$500k: every seeded comp plan clears at every loan size, incl. 275 bps which previously failed even at $400k; no file that works today prices differently. Honest residual: comp ALONE above ~300 bps still cannot be rescued (a comp-plan problem) and the gates still refuse. **F-18 (High):** `evaluatePointsAndFeesFloor` had ONE consumer (submission), but the compensation election decides the QM outcome and freezes at LE issuance — the check ran after its own remedy expired. The election now scores before writing (422 `qm_points_and_fees_exceeded` with the ceiling + remedy, audited as `compensation_election_refused`), and `GET .../compensation/qm` feeds the staff card the ceiling BEFORE a rate is attempted; the card disables Save above it, mirroring the 422 as it already mirrors the post-LE 409. **F-19 (Low):** the tax service fee was a prepaid finance charge in `knownPrepaidFinanceCharges` + `apr.ts` but ABSENT from the points-and-fees floor — the same $100 shrank the cap without counting against it. Fixed structurally: ONE `PLATFORM_FINANCE_CHARGES` list feeds numerator and denominator, so the CLASS is closed, not the instance. No new regulatory reading — it propagates the assertion the code already made in two places to the third. **Admin surface:** `/admin/pricing-policy` — the fee schedule and the wholesale comp bands (already DB-backed, previously no surface) are now editable without a deploy. Publishing is **append-only** (supersede + insert in one transaction; a partial unique index makes a concurrent double-publish fail loudly), requires a reason, and is audited. `services/loanCosts.ts` stays **pure** — the schedule is a PARAMETER everywhere; `services/platformFeeSchedule.ts` is the single impure edge, cache invalidated on publish, falling back to the compiled-in baseline if the read fails so pricing never hard-fails on a config table. **§9 security review** (role/permission-gate trigger) run before merge, **no HIGH/MEDIUM findings** — recorded in the PR body and in the audit log. | **Migration `0047_platform_fee_schedules` applies on merge** via `migrate-prod` — **prod migration HEAD 0046 → 0047**. NEW TABLE ONLY: expand-only, idempotent (`CREATE TABLE IF NOT EXISTS`), **no backfill and no seed row** (an empty table means "use the compiled-in baseline", so seeding the constants would fork the baseline in two places). Every column NOT NULL, safe precisely because a new table has no existing rows — **not a contract migration**, so the §Contract-migrations prod probe does not apply. Renumbered from `0046` during the merge: `main` landed `0046_document_borrower_description` while this branch was in flight and two migrations claiming idx 46 would have broken the ledger. No env-var change; no new dependencies. | `pnpm check` clean; **2,215 tests green** (1,954 node + 261 client), incl. 53 new tests across `platformFeeSchedule.test.ts` + `compensationElectionQmGate.test.ts` and 14 component tests on the admin panel; all seven repo guards at baseline plus `migration-ledger-guard` (48 migrations, contiguous idx 0..47); **gate green on the PR** (run 31031315350, all 17 steps incl. production build + Vercel deployability). Post-merge `/api/health` probe recorded below. **Vercel preview was rate-limited** (`api-deployments-free-per-day`, >100/day on the free plan) — a non-required check, unrelated to this branch. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>` restores the fixed fee schedule AND the non-QM dead band, and re-opens F-18/F-19 (don't revert without a replacement). The `platform_fee_schedules` table is additive and unread after a revert, so it can be left in place. |
| 2026-08-05 | **PR #374 — fix(mismo): roadmap L6-fix — every structural XSD violation except escalation U-1 cleared; baseline 9→2 on both legs; this ledger row rides in the same PR.** Compliance-first method, per CLAUDE.md: every corrected element name/path was **verified by extracting the content model from `docs/fannie-mae/schemas/uldd-phase5-extension/MISMO_3_0.xsd` and re-running xmllint locally** — never taken from the error message alone, never from memory. Fixed in `server/mismo.ts`: **(1)** `SUBJECT_PROPERTY` → `COLLATERAL/PROPERTIES/PROPERTY`, with estimated value + sales contract relocated to `PROPERTY_VALUATIONS/PROPERTY_VALUATION/{PROPERTY_VALUATION_DETAIL, SALES_CONTRACT}` (the schema has no SUBJECT_PROPERTY and no SALES_CONTRACT_DETAIL); **(2)** typed loan identifiers — `LenderLoanIdentifier` / `MERS_MINIdentifier` (no generic LoanIdentifier+Type pair exists); **(3)** `NAME` names + sequence (`FirstName`/`LastName`/`MiddleName`/`SuffixName`; LastName before MiddleName); **(4)** `CONTACT_POINTS` before `NAME` in `INDIVIDUAL`; **(5)** `BorrowerSSNIdentifier` **dropped** — the element does not exist in BORROWER_DETAIL; the SSN's one schema-valid home is `TAXPAYER_IDENTIFIERS`, already emitted, so this also narrows the PII surface; **(6)** employer name/phone into `LEGAL_ENTITY/{CONTACTS, LEGAL_ENTITY_DETAIL/FullName}`; **(7)** `DEAL` child sequence (ASSETS, COLLATERALS, LIABILITIES, LOANS, PARTIES); plus three violations previously **masked** by invalid parents: `ADDRESS` order (CountryCode/PostalCode before StateCode), `EMPLOYMENT` order (EndDate before MonthlyIncomeAmount), `PropertyUnitCount` → `FinancedUnitCount`. **U-1 deliberately untouched** (`AUTOMATED_UNDERWRITINGS`/`UnderwritingDecisionType` — AUS names pend ULDD data-dictionary confirmation). | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change. The MISMO package is generated at submission time and hashed per submission — previously-generated stored packages keep their recorded hashes; new generations produce the corrected structure. | `pnpm check` clean; **xmllint re-run on BOTH purposes locally: only the two U-1 elements remain**; baseline arrays shrunk 9→2 with the fix inventory recorded in the test; full suites **1,831 node + 240 client green** (mismoExport/mersMin/xsdValidation all pass); gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>` restores the schema-invalid structure AND the SSN duplication (don't). |
| 2026-08-05 | **PR #373 — refactor(auth): roadmap CH-4, fifty hand-rolled admin checks behind ONE predicate; this ledger row rides in the same PR.** The object-level `role === "admin"` string comparisons had grown 37→**50 across 19 server files** since filing. New `shared/roles.isAdmin(user)` (null-safe — no user is never an admin); every occurrence replaced **behavior-identically**: `x.role === "admin"` → `isAdmin(x)`, `!==` → `!isAdmin(x)`. The single semantic delta is strictly fail-closed — `req.user!.role` sites that would have THROWN on a missing user now deny 403. `server/auth.ts`'s pre-existing `isAdmin` Express **middleware** keeps its exported name (the predicate is imported aliased there); route-level `requireRole("admin")` gating is untouched everywhere. **§9 (role-gates trigger) reviewed in-session, no findings:** zero checks added, removed, widened, or narrowed — comparison expressions only, verified hunk-by-hunk. `tests/adminPredicate.test.ts` pins the predicate's truth table and walks all of `server/` failing on any new raw comparison, so the drift can't restart; two existing source-guard pins updated to the predicate form. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **1,831 node + 240 client green** (incl. the new truth-table + server-walk guards); gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (pure expression-level refactor; reverting restores the fifty string compares). |
| 2026-08-05 | **PR #370 — feat(funnel): roadmap A1, server-side draft persistence + a latent submit defect fixed; this ledger row rides in the same PR.** The funnel's "finish later" state was localStorage-only. The read half existed (`draft/latest` + restore banner + a PATCH-into-draft submit branch); what was missing was the WRITER — and reading the submit path exposed a latent defect: the PATCH-submit branch (taken whenever a prior draft existed) updated fields but **never flipped status** — no analysis, no stamps, no notifications; the "submitted" application stayed in `draft` and the borrower was navigated to loan-options anyway. Shipped: **(1)** `POST /api/loan-applications/draft` — idempotent find-or-create of the user's ONE bare container behind the intake gates (prelaunch + intake-pause; no figures accepted — answers only through the existing validated drafts-only PATCH, which keeps its licensed-state gate and TRID evaluation). **(2)** `useServerDraftAutosave` — debounced (2.5s) authenticated autosave; empty answers OMITTED so partial progress can't blank earlier ones; silent failures by contract (localStorage stays the fallback). **(3)** The client always POSTs now; the intake route consumes the user's draft server-side — fields updated, then draft→submitted through `updatePipelineStage` (the statusVocabulary single-writer rule), then the FULL intake side-effect chain (A3 promotion, TRID, consent evidence, invite link, LO auto-assign, activity, notifications, finalizeIntake). TRID note: the funnel never collects the property address pre-submit, so a draft cannot accumulate the §1026.2(a)(3) six pieces — draft saves cannot start the LE clock. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **9 new tests** (`tests/funnelDraftPersistence.test.ts`: endpoint gates + bare-container pins, draft-consumption + single-writer pins, no-PATCH-submit-branch pin, hook route/silence contracts, pure payload-filter unit test); funnel client suites 37 green; full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>` restores localStorage-only persistence AND the draft-stuck-submit defect (don't revert without a replacement fix for the latter). |
| 2026-08-05 | **PR #368 — feat(staff): roadmap ARC-2, pricing-intelligence panel binds the market-data moat; this ledger row rides in the same PR.** The three staff-only market-data endpoints (competitor benchmark over public HMDA, undercut quote, Fannie performance risk profile) had zero client references since they shipped. New `/pricing-intelligence` page (sidebar-linked, Operations): borrower-profile form → **us-vs-market card** (our best executable rate vs the competitor median, SIGNED delta in bps — negative = we win, no absolute-value flattering — with p25/p75/loan-count/geo-fallback context and the service's advisory note verbatim) + **risk-band history card** (default / 90-day-delinq / prepay rates, labeled "historical Fannie Mae performance, not a prediction for this borrower"). New `ROUTE_GATES.marketData` mirrors the server's EXACT role list (internal staff **minus closer** — the server 403s a closer, so the nav/router never offer it). A 404 renders the endpoint's own "run the ingest script" hint, never an empty chart. Advisory posture preserved: no write path — rate changes go through rate sheets (the service's Reg Z rationale). | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **3 colocated tests green** (form-first render, signed delta + advisory note verbatim, history-not-prediction labeling); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (page + route gate + sidebar link + tests; reverting re-orphans the three endpoints). |
| 2026-08-05 | **PR #367 — feat(cockpit): roadmap LO-M17, one-click document-request drafts; this ledger row rides in the same PR.** The "No-Stall Guarantee" workflow's missing piece: `fileHealth.reasons` told the LO *what* was blocking a file but nothing turned that into an approvable ask. Shipped: pure `server/services/docRequestDraft.ts` reusing the A8 nudge planner's missing-document rules (outstanding upload-ask conditions only; a **rejected** upload does not satisfy; alias-aware matching; one deduplicated ask per document type carrying every condition title) + read-only `GET /api/staff/applications/:id/doc-request-draft` behind the cockpit's exact role gate + assignment scope + `DocRequestDraftDialog` on the Command Center's active-borrower pane (pre-filled items labeled with the conditions they clear, per-item exclusion, optional note, explicit approve). **Deliberate boundary: no new send surface** — each approved item goes through the EXISTING `POST /api/messages` document_request path, inheriting its staff gate, borrower notification, and content-free email unchanged (source-guarded: the draft route is read-only). | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **8 node tests** (builder rules incl. rejected-doesn't-satisfy + alias dedupe; route-gate and send-path source guards) + **4 client tests** (pre-fill with condition labels, exclusion arithmetic + zero-selected disable, empty-draft-is-normal, approval-gated copy); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (service + read-only route + dialog + tests; reverting returns doc requests to hand-composed only). |
| 2026-08-05 | **PR #366 — feat(intake): roadmap A3 resolved — the active_buyer split gets its missing promotion writer; this ledger row rides in the same PR.** The 2026-07-08 audit's "retire or wire" question answered **wire**: the split is a real UX distinction (aspiring_owner → Explore/Get-Ready sidebar with gap calculator + DPA wizard; active_buyer → My-Mortgage sidebar with To-Do/Documents/Verification), but nothing ever promoted anyone — every real borrower kept the renter sidebar forever, and the admin dashboard's activeBuyers stat counted a permanently-zero cohort. Fix: at `POST /api/loan-applications` creation, exactly `aspiring_owner` → `active_buyer` — **self-only** (`userId = user.id`; no cross-user write path exists), audited (`user.role_change` with reason `auto_promotion_on_application`), idempotent, non-fatal to intake. **§9 review run in-session, no findings:** both roles are CLIENT_ROLES with zero `requireRole` references anywhere in `server/routes/` — the promotion changes navigation cohort + admin stats, never privileges — and the new test walks the routes tree to keep that invariant enforced (a future role gate on either client role fails the suite, forcing re-review as a privilege change). | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change. Existing aspiring_owner users with applications are NOT backfilled — they promote on their next application, or an admin can flip them; a data backfill would be a prod write outside the migration lane. | `pnpm check` clean; **4 new tests** (promotion guard + audit pins, non-fatal wrapper, both-roles-in-CLIENT_ROLES, the no-role-gates-on-client-roles routes walk); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route addition + tests; reverting stops future promotions, already-promoted users keep working — active_buyer was always a valid role). |
| 2026-08-05 | **PR #365 — chore(server): roadmap OPT-7 + OPT-8 duplicates deleted, OPT-cleanup executed; this ledger row rides in the same PR.** Three built-but-never-triggered surfaces removed outright: **(1)** `optimizationEngine.checkSlaBreaches`/`sendSlaAlerts` (OPT-7) — CS1's closure made taskEngine the source-guarded single owner of scheduled SLA enforcement, and `sendSlaAlerts` emailed borrowers task-nag content with none of the outbound rails; **(2)** `checkRefinanceOpportunities`/`processPostClosingLifecycle` (OPT-8) — strictly worse than the incumbent `lifecycleEngine` cron: hardcoded 6.5% market rate, no dedup on `refi_alerts` inserts, and refi emails with **no F-8 clawback-window suppression** (a run would have urged borrowers to refinance loans still inside the EPO window — the exact self-cannibalization the audit guarded against); **(3)** the dead `server/routes/optimizations.ts` (all 12 endpoints, zero client callers verified) + its `routes.ts` registration (OPT-cleanup) — which also removes the HTTP trigger for the compliance-blocked `sendReEngagementEmails` (OPT-2's function stays in the engine, unwired). OPT-3/6/10 functions remain in the engine awaiting product decisions; deleting the caller-less route forecloses none of them. Orphaned imports pruned. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change. Net **−420 lines** of live-but-untriggered surface. | `pnpm check` clean; `taskEngineSlaSeed` OPT-7 single-channel guards still green; full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>` restores the duplicates and the dead route (there is no reason to). |
| 2026-08-05 | **PR #364 — test(pipeline): roadmap CH-6 closed — `updatePipelineStage` (THE single status writer) finally under test; this ledger row rides in the same PR.** Finding F-015's gap: the 897-line pipeline engine's core function — the one chokepoint every status change funnels through — had zero dedicated coverage. `tests/pipelineEngineStageTransitions.test.ts` adds 14 mocked-storage tests (no DB): unknown-status rejection **before any storage read**, idempotent re-assert as a no-op, forbidden transitions throwing `PipelineTransitionError` with the allowed targets attached, `force` bypassing validation while keeping side effects, per-stage milestone stamping, the three **HMDA Reg C action-taken rules** — funded→code 1 + homeowner graduation, denied-with-reasons→code 3 + reasons, **denied-without-reasons fabricating NO LAR entry** (the automated path must finalize through the formal adverse-action flow), withdrawn→code 4 — and the never-fail-the-transition contract (graduation/task-event failures swallowed by design). Plus the pure `conditionsToRevertAfterRejection` re-arm logic (rejected-doc reverts, still-satisfied keeps, submitted-only scope). Zero production-code changes. | **None** — tests only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change. | `pnpm check` clean; 14 new tests green; full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, trivially safe (test file + vitest include entry only). |
| 2026-08-05 | **PR #363 — test(rules): roadmap CH-5, first dedicated coverage for the DSL rule engine; this ledger row rides in the same PR.** `server/services/ruleEngine.ts` (the condition/action evaluator behind `/api/underwriting-rules/execute`) had zero tests for its whole life. `tests/ruleEngine.test.ts` adds 14 characterization tests through a stub IStorage — no DB, no fixtures — pinning the decision-adjacent semantics that must not drift silently: numeric operators are **fail-closed on non-number context values** (no coercion), unknown operators evaluate false rather than throw, strict EQUALS, inclusive BETWEEN, EXISTS treats null like undefined, dotted-path traversal never throws, AND/OR groups nest, execution runs in ascending priority (default 100), `stopOnMatch` halts only after a **matched** rule, inactive/unknown rules are filtered on the ruleIds path, actions execute only on match (unknown action types pass through raw), and every evaluated rule writes an execution log, matched or not. Zero production-code changes — pure test addition + vitest registration. | **None** — tests only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change. | `pnpm check` clean; the 14 new tests green; full node suite green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, trivially safe (test file + vitest include entry only). |
| 2026-08-05 | **PR #362 — feat(reports): roadmap G-C, cycle-time / pull-through report; this ledger row rides in the same PR.** The acquisition playbook's gates (>70% pull-through, <12-day cycle time) were structurally unreportable — status history lives in `audit_logs` as raw rows. Shipped: pure `shared/cycleTimeReport.ts` + `GET /api/reports/cycle-time?days=` (admin, window clamped 7–365) + the Funnel card on `/admin/financial-reports` (endpoint ships WITH its consumer, keeping `UNCONSUMED_CAPABILITIES.md` empty). Honesty rules are properties of the payload, pinned by tests: **two named pull-through denominators** (resolved-only = the gate's number; overall = dragged by in-flight files, and the note says so), **null-never-zero** (nothing resolved → null, rendered "—"), funded files with **no usable funding transition** in the audit trail counted in pull-through but excluded-and-counted in cycle time, and an earlier-than-creation transition treated as corrupt/unmeasured rather than a negative-day close. Cycle time = application `createdAt` → FIRST `loan_application.status_changed` into `funded` (median + p90). Terminal vocabulary pinned to `LOAN_APP_TERMINAL_STATUSES`. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **10 new node tests** (nulls, denominators, median/p90, unmeasured + corrupt-data honesty, vocabulary pin, admin-gate + clamp source guards) + **2 new client tests** (gates named in hints; null renders "—" with the note verbatim); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (shared module + route + admin card + tests; reverting makes the funnel gates unreportable again, nothing else). |
| 2026-08-05 | **PR #361 — feat(jobs): roadmap CS1 closed — the SLA escalation check is finally scheduled; this ledger row rides in the same PR.** The item went stale in the useful direction: the seed half (empty `sla_class_configs`/`task_type_sla_mapping`) was already fixed ~07-17 (`server/seedData/taskEngineSla.ts`, boot-seeded, statically pinned) — but nothing ever RAN the breach check, so a task could sail past its `slaDueAt` forever unless an admin manually POSTed `/api/task-engine/run-escalation`. Shipped: `GET /api/jobs/task-escalation` in the jobs.ts dual-trigger shape (Vercel cron via `CRON_SECRET`, or admin session with audit entry) calling `taskEngine.runEscalationCheck()` — level bump + task audit log + configured escalation actions per breached task — plus the daily `vercel.json` cron (13:45 UTC). **Single-owner rule enforced in tests:** taskEngine is the one scheduled SLA channel; optimizationEngine's `checkSlaBreaches` duplicate (roadmap OPT-7) is source-guarded out of the jobs surface. Honest caveat in-route and in the roadmap: daily cadence means S0's 15-minute escalation window is aspirational until the cron tier supports sub-daily schedules. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); one `vercel.json` cron added; no env-var change (reuses `CRON_SECRET`). | `pnpm check` clean; `tests/taskEngineSlaSeed.test.ts` extended 8→11 (route + cron presence pins, the OPT-7 never-wire-the-duplicate guard); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route + cron + tests/docs; reverting returns SLA enforcement to manual-only). |
| 2026-08-05 | **PR #360 — feat(compliance): roadmap CS2, automated founder escalation on discrimination/credit-error language in borrower messages; this ledger row rides in the same PR.** Closes the gap the escalation playbook documented: a borrower message alleging discrimination or a credit-reporting error landed in the assigned LO's normal inbox with no priority bump or founder visibility. New pure scanner `shared/compliance/complaintEscalation.ts` (the playbook's trigger vocabulary verbatim — ECOA protected-ground phrasing, "unfair", FCRA dispute language, identity theft — deliberately **over-flagging**, since the scan gates only an internal escalation), wired on the borrower-side send path of `POST /api/messages`: a match writes a `complaint.flagged` audit entry + an admin-only `complaint_escalation` in-app notification. Data minimization is load-bearing: the notification body carries matched categories, **never the borrower's text** — the verbatim record the playbook's step 3 requires is the `team_messages` row itself, referenced by entityId, behind login. The scan never blocks, alters, or delays the send (fire-and-forget), and the borrower response is unchanged (no tip-off). **§9 note:** deliberately NO email leg — that would be a new outbound-messaging surface (a §9 review trigger) and would tempt message content into email; in-app-only means no role gate, PII path, or outbound surface changed. Playbook's remediation section updated to SHIPPED with the human/code boundary stated. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies (`notifications.type` is a free varchar; unknown types render with the panel's default icon). | `pnpm check` clean; **26 new tests green** (`tests/complaintEscalation.test.ts`, registered in the vitest include list: every playbook phrase family flags with its category, routine traffic doesn't, case-insensitivity, both-categories ordering, source guards on the fire-and-forget wiring + the admin-only and no-message-text contracts); full suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (shared scanner + service + route wiring + tests/docs; reverting restores the silent-inbox gap the playbook documents). |
| 2026-08-05 | **PR #358 — perf(routes): roadmap CH-2 (2 of 3 loops) + CH-3 verified already done; this ledger row rides in the same PR.** Two of the three N+1 per-row fetch loops converted to the `/api/dashboard` `inArray` house pattern: `/api/agents/search` enriches all agents from **one** `getUsersByIds` batch (was one `getUser` per agent), and `/api/my-referrals` loads every referred user's applications in **one** new `storage.getLoanApplicationsByUserIds` query (was one query per referral; the batched method keeps the newest-first ordering so per-user `apps[0]` is still the latest application — same contract as the single-user variant). Behavior-identical responses; query count now O(1) instead of O(rows). **Deliberate residual, recorded in the roadmap:** the third loop (per-application MISMO validation on the compliance dashboard, now `underwriting/compliance.ts`) is NOT converted — `validateMISMOCompleteness` performs 5+ storage reads per application internally, so batching it is a compliance-sensitive URLA-validator restructuring, not a mechanical swap. CH-3 (`nanoid` explicit dependency) verified already satisfied in `package.json` and checked off. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **6 new source-guard tests green** (`tests/nPlusOneBatching.test.ts`, registered in the vitest include list: batch methods present, no `map(async` N+1 shape, ordering + empty-list contracts on the new storage method); full unit suites green pre-PR; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route + storage-method change only; reverting restores the per-row loops, not a correctness change). |
| 2026-08-05 | **PR #356 — fix(funnel): roadmap LO-M16, program-aware PMI in the advisory payment estimate; this ledger row rides in the same PR.** The ticket's premise was corrected on verification (recorded in the roadmap checkoff): `0.0125` is the platform-standard taxes+insurance-only constant, so PMI was never in the funnel estimate for anyone — the real defect was conventional low-down estimates *omitting* PMI while the adjacent copy promised "20% down avoids PMI, saving you money each month." Fix in `preApproval/AdvisoryPanel.tsx`: illustrative conventional PMI (0.5%/yr of loan — ScenarioDesk's existing figure, not a new invention) added when LTV > 80 unless `isVeteran && loanPurpose === "purchase"` (the exact branch the advisory copy keys on), so the number and the words can no longer disagree in either direction; the payment disclaimer names PMI only when included and states the VA no-PMI case explicitly. Advisory/illustrative surface only — no underwriting or pricing engine math touched. | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0045`**); no env-var change; no new dependencies. | `pnpm check` clean; **4 new colocated tests green** (number-copy agreement in all four quadrants: low-down conventional includes PMI, VA purchase $0-down excludes it, VA *refinance* is not the VA-purchase branch, 20%+ down excludes it); full client suite 231 green; gate green on the PR. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client-only; reverting restores the PMI-omitting estimate). |
| 2026-08-04 | **PR #351 — fix(trid): two disclosure defects in #342's F-4/F-9 work, found by an independent post-merge review; this ledger row lands in a follow-up docs-only PR (same session), #351 having merged without one.** **(1) Delivery-route ordering:** `markRevisedLeDelivered` ran **before** `reconcileDisclosure`, closing open change-of-circumstance records before the reconciler read them — the `increase_justified` verdict was unreachable (every justified increase misread as `cure_required`) and, worse, on `blocked_no_coc` the route stamped a §1026.19(e)(4)(i) revised-LE delivery record while serving the held baseline. Fixed: reconcile first; `blocked_no_coc` returns before any stamp. **(2) Simulated-fee leak:** the F-9 actual-fees bridge didn't skip `simulated` cost entries, so a $5 simulated credit pull could become the disclosed zero-tolerance credit-report fee. Fixed: `resolveActualFeesFor` skips `simulated`. Both pinned in `tests/leDisclosedFeeProvenance.test.ts` (disclosure pins + source-order guards); correction notes added to the audit log's F-4 and F-9 remediation sections in the follow-up PR. **📝 Ledger corrections (append-only — rows below stand as written):** the "PR #351" row below is actually **PR #352** (the Financials tab; this PR took #351 first), and the "PR #349" row below is actually **PR #350** (`af0d0f7` — #349 was the Homiquity brand-consolidation chore). *(Observed: #347 — resources CTA fix — and #349 — brand consolidation — merged from parallel sessions without ledger rows; noted here per the #324 precedent rather than backfilled by a session that didn't ship them.)* | **Migration `0045` auto-applied to prod on merge** — `migrate-prod` green 2026-08-04 20:30:03Z, log reads `applying 0045_rate_lock_confirmed_by_fk ... done / applied 1 migration(s)`: FK `rate_locks.confirmed_by_user_id → users.id` added **`NOT VALID`** (validates new writes only — no scan/lock risk on a populated table) inside an idempotent DO block. Prod migration HEAD **`0044` → `0045`**. No env-var change. | Gate green on the PR; the three load-bearing claims re-verified against code before merge (route order at `a1f031c`, absent `simulated` filter, `migrate-prod.cjs` `appliedWhens` dedupe making the renumber-safe claim true); post-merge `migrate-prod` job log read directly (above). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit `a86d2bd` — `git revert` restores both defects (do so only with a same-PR route fix); the `NOT VALID` FK stays applied by design (expand-only contract). |
| 2026-08-04 | **PR #351 — feat(staff): per-file Financials tab on the borrower file; this ledger row rides in the same PR.** The last two `UNCONSUMED_CAPABILITIES.md` entries consumed, emptying the register's open list the same day it opened: `FinancialsTab` (new BorrowerFile tab, staff-only page) renders **(1)** the F-4 TRID tolerance posture from `GET …/le-tolerance` — verdict badge, cure exposure "if closed on today's numbers", per-line increases, with the module's caveats rendered (read-only, never creates a baseline; a redisclosure gate, not a legal cure computation; "no baseline yet" shown as the normal pre-disclosure state, not an error) — and **(2)** the F-11 per-file cost ledger from `GET`/`POST …/costs` — entries with Metered/Staff/Simulated provenance, simulated spend labelled and excluded from totals, staff cost entry permitting negatives (the append-only ledger's reversal) but not zero; a booked actual invalidates the LE query (feeProvenance override). Two `loanApplicationKeys` factory entries added (`leTolerance`, `costs` — guard:querykeys clean). | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0044`**); no env-var change; no new dependencies. | `pnpm check` clean; **6 new colocated tests green** (no-baseline-is-normal pin, cure surfacing, read-only caveats, simulated-cost labelling, reversal contract copy); full suites + guards green pre-PR; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client tab + factory keys + docs; no schema/DB change; reverting re-opens the two register entries). |
| 2026-08-04 | **PR #349 — feat(admin): financial-reports page; this ledger row rides in the same PR.** The consumer for the two company-level report endpoints #342 shipped (F-6/F-8/F-11/F-13): `/admin/financial-reports` (admin-gated via the existing `AdminPage`/`ROUTE_GATES.adminOnly` wrapper, sidebar-linked) renders revenue + pull-through + approved-lender capacity, unit economics, EPO clawback exposure, remittance discrepancies, and the contingent-liability register. The audit's honesty rules are load-bearing in the UI and test-pinned: gross margin renders with the endpoint's own upper-bound notes verbatim; the liability sum is labelled "not a total" with the unquantified count beside it; null metrics (pull-through with nothing resolved, unpriceable exposures) render "—", never a fake zero; zero approved lenders renders as the blocked state it is (F-5). `UNCONSUMED_CAPABILITIES.md`: both company-report entries moved to Closed as **Consumed**; the two per-file surfaces (le-tolerance, cost ledger) remain open. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0044`**); no env-var change; no new dependencies (types imported from `@shared`). | `pnpm check` clean; **8 new colocated tests green** (null-≠-zero pins, floor labelling, F-5 blocked-state copy, short-pay row, assumed-window + indeterminate clawback badges, loading skeletons); full suites + guards green pre-PR; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client page + route + sidebar link + register/ledger docs; no schema/DB change; reverting re-opens the register entries). |
| 2026-08-04 | **PR #348 — feat(staff): compensation-election card on the borrower file; this ledger row (and #342's, which merged without one) rides in this PR.** The client surface for `PATCH /api/loan-applications/:id/compensation` (#342's F-1 route), closing the gap `UNCONSUMED_CAPABILITIES.md` flagged as load-bearing-without-a-UI: the Loan Estimate refuses to generate until a compensation model is elected, so with no surface no file could price without a hand-made API call. `CompensationCard` sits first and full-width on the borrower file's Overview tab (no-election is the file's blocking state); server-gate mirroring both ways — post-LE *change* shows the changed-circumstance lock note (the route's 409 `compensation_locked_after_le`), post-LE *first* election stays offered (the server only blocks changes); no new GET (columns ride the staff detail payload); election row moved to the register's Closed entries as **Consumed**. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0044`** after #342); no env-var change; no new dependencies (model selection is two `aria-pressed` toggle buttons — the ui kit has no radio-group). | `pnpm check` clean; **1,720 node + 209 client green** (7 new colocated tests: shared-vocabulary pin on `COMPENSATION_MODELS` (the #247 phantom-status class), blocking copy names §1026.36(d)(2), the 409 mirror in both directions, bps 0–1000 validity gate); token/query-key/doc-freshness/KB-index guards at baseline; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; **merge** commit — revert restores the UI gap (election back to API-only), no schema/DB change. |
| 2026-08-04 | **PR #342 — the 2026-08-04 financial architecture audit: 13 of 16 findings closed + 4 process guards.** Full evidence: [the audit log](../logs/2026-08-04-financial-architecture-capital-structure-audit.md). Compensation-aware fee schedule + fail-closed QM floor (F-1/F-2, Reg Z §1026.36(d)(2)/§1026.43); lender-confirmed rate locks + persisted TRID tolerance baselines with cure detection (F-3/F-4); counterparty approval gate + two-sided compensation ledger (F-5/F-6); LE Section A origination-fee itemization + EPO clawback register with the refi-alert self-cannibalization guard (F-7/F-8); fee provenance tiers + per-file cost ledger with metered credit pulls (F-9 architecture/F-11); extension-fee payer attribution + corrected Reg Z total-loan-amount basis (F-10/F-12); contingent-liability register with `quantifiedFloor` semantics (F-13); `BUSINESS_CHANNEL` declaration + delivery-stack freeze ratchet (F-14 — decision stays founder-owned, [CHANNEL_DECISION.md](../governance/CHANNEL_DECISION.md)). Guards: compliance-critical field registry, `guard:docs` doc freshness, `UNCONSUMED_CAPABILITIES.md`, TEAM_PRACTICES §4 PR-size rule. **⚠️ Two standing flags:** production wholesale submissions are **blocked** until a lender flips to `approved` in `shared/wholesaleLenders.ts` (F-5, deliberate — no env-var backdoor), and **six regulatory-ledger entries carry 14–30-day review intervals** pending human verification against regulation text this environment could not reach. | **Migrations `0039`–`0044` auto-applied to prod on merge** (`migrate-prod` job green, 2026-08-04 19:59:49Z): lo_compensation columns, rate-lock lender confirmation, `loan_estimate_disclosures`, lender-submission compensation lifecycle, `loan_cost_entries`, extension-fee payer. All expand-only `ADD COLUMN IF NOT EXISTS`/`CREATE TABLE IF NOT EXISTS`, none backfills — prod migration HEAD **`0038` → `0044`**. No env-var change. | Gate green on the PR head after three merge-up rounds against a fast-moving main (React 19 bump re-verified); **1,720 node + 202 client green** at merge; all guards incl. the two new ones; `migrate-prod` job success verified post-merge. | [ROLLBACK.md](./ROLLBACK.md) §1; **merge** commit (12 commits preserved) — `git revert -m 1 833b550` reverts the code; migrations stay applied by design (expand-only, tolerated by the pre-#342 app — the DB_MIGRATIONS.md expand/contract contract). |
| 2026-08-04 | **PR #344 — fix(activities): legacy activity-description gating moved from a wall clock to a per-row writer-contract marker; this ledger row rides in the same PR.** #341 gated `status_change`/`document_uploaded` descriptions on a `created_at` cutover. Probing to tighten that cutover to the real deploy instant (`2026-08-04T19:05:04.679Z`, deployment `abf4abc` READY) showed the comparison is unsound: `created_at` is `timestamp without time zone` filled entirely by the column default (no writer sets it), so its zone is the writing session's — the dev branch reports **America/Chicago** — while readers parse in the Node process's zone. Live evidence: a row written **11 min after** the deploy stores as `2026-08-04 14:16:39` and reads back **49 min before** it to a UTC reader; the same 5-7h shift runs the leak direction for a western reader. #341's 2026-08-06 cutover was safe only by ~29h of slack. Replaced with a marker: `storage.createDealActivity` (the single insert path — all 44 writers funnel through it) stamps `writerContract: 1` into `metadata`, merged over caller metadata; the view shows a legacy-type description only when the marker is present. `metadata` is already embargoed, so the marker never reaches a borrower; marker-absence is the safe default (a chokepoint that stops stamping over-masks, never leaks). | **None** — `metadata` is an existing `jsonb` column; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0038`**); no env-var change. | `pnpm check` clean; unit **1,564 node + 186 client green** (new: near-miss marker rejection — wrong version/string/boolean/array/bare-string/null/undefined; a pin that timestamps do NOT gate the description in either direction; a source guard that the chokepoint stamps the marker and preserves caller metadata); live probe on :5003 (buyer vs admin, same application): staff waive with a sentinel → raw admin row carries `{notes, conditionId, writerContract: 1}`, borrower response contains none of the sentinel / `writerContract` / `metadata` / `Reason:`; probe condition reverted; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (shared view + storage chokepoint + tests, no schema; reverting restores the unsound clock comparison). |
| 2026-08-04 | **PR #343 — feat(staff): pre-approval letter card with role-gated revocation in the borrower file; this ledger row rides in the same PR.** The staff surface #340 deferred: `PreApprovalLetterCard` in the borrower file's Credit tab renders the file's letter with server-computed status (`LETTER_STATUS_BADGE` typed `Record<PreApprovalLetterStatus,…>`, test-pinned to the shared vocabulary — the #247 phantom-status class) and a revoke dialog gated three ways, each mirroring a server rejection: `canRevoke` = `CREDIT_DECISION_ROLES` (the 403, same wiring as `StatusUpdateDialog`), issued-only rendering (the 409 SQL guard), confirm disabled until the **shared** `letterRevocationSchema` passes (the 400 — schema moved to `shared/letters.ts` so dialog and route validate identically). Server delta: `letter-status` exposes `letterId` (the mutation target; UUID behind the existing access boundary), route validates via the shared schema. **§9: no trigger** — no gate added/modified, no PII change (recorded on the PR). | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0038`**); no env-var change. | `pnpm check` clean; **1,562 node + 194 client green** (8 new component tests: vocabulary pin, role-gate hiding, issued-only offering across all statuses, shared-schema confirm gate; +2 letters source guards → 27); token/query-key/schema guards at baseline; **live-verified on a worktree dev server**: revocation drove badge Issued→Revoked, `revoked_by`-stamped row + `letter_revoked` ledger + borrower notification + 409 on `letter-pdf`; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client card + response field + shared schema move; no schema/DB change). |
| 2026-08-04 | **PR #341 — fix(activities): borrower deal-activity feed masked behind a whitelist view; this ledger row rides in the same PR.** Closes the leak the §9 review of the borrowerConditionView PR flagged: `GET /api/loan-applications/:id` and `GET /api/dashboard` served `deal_activities` rows to client roles verbatim — staff clearance free text (`Reason: <clearanceNotes>` in the waive/not-applicable descriptions + `metadata.notes` on all three condition events) and wholesale-lender identity ("N condition(s) from \<lender\> logged" — borrower transparency doctrine); the table's `visible_to_roles` column was never wired. New `shared/borrowerActivityView.ts` (borrowerOfferView pattern): total column classification pinned against the live table; three type tiers — verbatim-scrubbed (`scrubLenderIdentity` + the now-exported `LENDER_IDENTIFIERS` alias vocabulary) / fixed-template / default-deny — `performedBy` viewer-scoped, `metadata`+`visibleToRoles` embargoed; applied for non-staff callers on both routes, staff keep full rows. Writer contract (same leak class, found in the audit): staff status-decision notes + deal-fell-through reason move from `description` into embargoed `metadata`; staff-owned task titles stop riding `document_uploaded` descriptions. §9 review's one Medium finding (historical pre-contract rows keep staff free text in `description`) resolved in-PR via a **writer-contract cutover**: pre-cutover `status_change`/`document_uploaded` rows (or missing `createdAt`) keep their always-derived titles and drop descriptions — history never rewritten, a slow deploy can only over-mask. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0038`**); no env-var change; no new dependencies. | `pnpm check` clean; unit **1,545 node + 186 client green** incl. new `tests/borrowerActivityView.test.ts` (column-classification gate, field-absence, full wholesale-catalog scrub probe, legacy-row masking, Reg N lint on template copy; registered in the vitest `include:`); live-probed on a worktree server (buyer@test.com vs admin@test.com, same application): admin-waived condition with a sentinel in `clearanceNotes` → raw admin row carries it in description+metadata, **entire borrower detail+dashboard responses carry zero occurrences**, internal `note`/`autopilot_review` rows dropped for the borrower while staff keep them; `/security-review` (§9 borrower-data trigger) → one Medium, **resolved in-PR** (outcome recorded in the PR body); gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (shared view + routes + tests, no schema; reverting re-opens the feed leak). |
| 2026-08-04 | **PR #340 — feat(letters): the pre-approval letter status lifecycle completed — staff revocation + layered expiry; this ledger row rides in the same PR.** The two follow-ups #332 recorded (adjudication §3.2, #329): `POST /api/pre-approval-letters/:letterId/revoke` — `requireRole(...CREDIT_DECISION_ROLES)` + deal-team boundary for non-admins (the statusDecisions class), atomic issued-only transition compared in SQL, required reason persisted to `revocationReason` (audit + `letter_revoked` ledger row only — a source guard pins that staff free text never reaches a borrower-facing response), in-app borrower notification (deliberately no email: no fitting `NotificationType` template — counsel-reviewable template is its own follow-up); `letter-pdf` now 409s (`letter_revoked`) on a revoked letter instead of serving silently. Expiry is **layered**: `effectiveLetterStatus` (`shared/letters.ts`, pure, injectable clock) computes expiry at read in `letter-status`/`prequal-status` — the API never asserts "issued" past expiration, whatever the sweep's timing — while `GET /api/jobs/letter-expiry` (the jobs.ts dual-trigger shape; daily `vercel.json` cron 12:30 UTC) persists `issued→expired` on **both** letter tables (SQL-compared, issued-only — terminal revoked/superseded provenance can never be clobbered by a clock) + batched `letter_expired` ledger rows. Expired letters stay downloadable (the document shows its expiration date on its face); revoked ones do not. Staff revocation UI ships separately. | **None** — no `migrations/` or `shared/schema/` change (the revocation columns already existed on `pre_approval_letters`; prod migration HEAD stays **`0038`**); one `vercel.json` cron added; no env-var change (reuses `CRON_SECRET`). | `pnpm check` clean; full unit suite **1,540 green** on the rebased tree incl. `letterIntegrity.test.ts` extended 4→24 (helper unit tests — boundary instant, revocation-wins-over-expiry — + source guards: role gate, exactly two SQL issued-only transitions (supersede + revoke), reason required and never echoed, revoked-pdf 409, computed status at both status endpoints, sweep issued-only on both tables, cron actually scheduled in `vercel.json`); **§9 role-gate trigger → security review run: no HIGH/MEDIUM findings** (recorded on the PR); gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route/service/shared-helper/tests, no schema; the revert removes the cron entry with it). |
| 2026-08-04 | **PR #336 — fix(underwriting): the six legacy engine-calculation routes staff-gated; this ledger row rides in the same PR.** Hardening leg of #324's adjudication §3.1: `calculate-income/-assets/-liabilities/-dti`, `check-property-eligibility`, and `calculate-pricing` sat behind bare `isAuthenticated` with an owner-passing access check and zero client callers — `calculate-income` handed a borrower computed qualifying-income figures over unverified self-stated inputs (the #314 §6.1 binding-rejection class; Reg N §1014.3(q)), `calculate-pricing` LLPA-adjusted pricing. Now `requireRole(...FINANCIAL_VERIFICATION_ROLES)` (the verify-financials reviewer boundary; deal-team scoping unchanged via `getLoanApplicationWithAccess`), same class + idiom as #317's lender-match fix. Source guard added to `tests/routeGateDrift.test.ts`; `tests/pricingUnderwriting.test.ts` engine calls moved to the admin fixture + a borrower-403-on-all-six HTTP test. Borrower income transparency ships separately via #331's whitelisted DTO, never these routes. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0038`**); no env-var change; no new dependencies. | `pnpm check` clean; unit **1,465 node + 164 client green** (incl. the new source guard); full integration suite vs the :5002 worktree server **147/147 green** incl. the new borrower-403 test; live probe: borrower `calculate-income` on own application → **403**, admin → **200**; `/security-review` (§9 role-gate trigger) → **no findings** (strict access narrowing, no caller class gains, no existence oracle); gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route + tests only; reverting re-opens the borrower reachability §3.1 documents). |
| 2026-08-04 | **PR #332 — fix(letters): pre-approval PDF regeneration renders from the stored letter row; supersede-on-reissue; this ledger row rides in the same PR.** Hardening leg of #329's adjudication §3: the `letter-pdf` regeneration fallback had rebuilt from **current** application data + **today's** advertised rate under the original letter number/date, minted an ungated fresh letter when no row existed, and stamped the downloader's name as borrower — it now renders strictly from the `pre_approval_letters` row via the pure `letterDataFromStoredRow` (prequal's existing pattern) and 404s when nothing was issued; `generate-letter` 500s if the row insert fails (an unrecorded letter is not issued) and flips prior issued letters to `superseded` (+`supersededAt`/`supersededBy`, audited) — first writer for the previously zero-writer lifecycle (revocation route + expiry sweep remain recorded follow-ups). Conditions block shared via `STANDARD_PRE_APPROVAL_CONDITIONS`. | **None** — no `migrations/` or `shared/schema/` change (dormant columns gain a writer; prod migration HEAD stays **`0038`**); no env-var change. | `pnpm check` clean; unit suite 30 files / 177 tests green incl. new `tests/letterIntegrity.test.ts` (pure regen-builder coverage — issued terms verbatim, no live financial detail, real `%PDF` render — + source guards: 404-on-no-row, single BN- minting site, single pricing call site, supersede writer, loud insert failure; registered in the vitest `include:`); `licensedStates` letters guard still green; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (route/service/test change only, no schema). |
| 2026-08-04 | **PR #329 — docs-only: the "cross-sector fintech frameworks" pitch adjudicated; nothing adopted for build; new binding restatement — no machine-issued financial attestations to third parties; this ledger row rides in the same PR.** New memo [2026-08-04-cross-sector-fintech-frameworks-pitch-adjudication.md](../logs/2026-08-04-cross-sector-fintech-frameworks-pitch-adjudication.md) (standing protocol #192 → #314 → #324): Stripe/Shopify revenue-telemetry qualification, the "30-second audit → instant rock-solid pre-approval", the asset-depletion "certificate in seconds" (its ÷360 divisor conflicts with the quarantined A.2 84/120-month methodology — both uncited), and the live seller-facing proof-of-funds certificate all rejected or deferred under standing rulings with reopen gates recorded; the one genuinely new idea — **debit-side MCA-stacking detection** (recurring MCA-remittance debits ⇒ undisclosed liability; covered by neither S-03 credit-report-side nor §5.2 inflow-side) — parked at the #238 §5.2 activation gate. Defects found in passing → follow-up letters hardening PR this session: pre-approval `letter-pdf` regeneration rebuilds from **current** application data + today's rate under the original letter number (and mints an ungated fresh letter when no row exists); the letter status lifecycle (`revoked`/`superseded`/`expired`) has zero writers. | **None** — docs-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0038`**, landed with #323); no env-var change. | kb-index-guard **PASS** (106 docs, no dead links); gate green on the PR; post-merge `/api/health` probe per the section above (docs-only ⇒ no runtime delta). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-08-04 | **PR #324 — docs-only: the "sovereign underwriting stack" pitch adjudicated; F12 IVES track adopted; §9 gains the New-PII-sub-processor trigger; this ledger row rides in the same PR.** New memo [2026-08-04-sovereign-underwriting-stack-pitch-adjudication.md](../logs/2026-08-04-sovereign-underwriting-stack-pitch-adjudication.md) (standing protocol #192 → #206 → #252 → #314): the pitch's "core IP" (tax parsing + SAM/1084 engine) already ships richer — its formula omits the B3-3.6-03 subtractions and the B3-3.5-01 loss doctrine, so as pitched it would **overstate** qualifying income; R2/Workers replatform, Google Document AI (second OCR — #206 §4.2), Python-FastAPI-on-Replit stack (#192 §4.4), the Non-QM auto-router (#238 Appendix-A quarantine), and the instant borrower income card (#314 §6.1 binding) all rejected with reopen gates recorded. The one extraction: CTO_ROADMAP **F12 — IRS IVES transcript access (4506-C → A2A)** — founder enrollment + counsel gated, then a simulated `vendors.ts` adapter ($4/transcript + Taxpayer First Act A2A API verified on irs.gov this session). NON_W2 research briefing gets a four-claim supersession banner (EXTRACTION_SIMULATE is tax-only; rental `appliedToDti` dynamic since #241; S-05/S-06 apply since #241; S-07 shipped #246/#315). Defect found in passing → follow-up hardening PR: staff-gate the six borrower-reachable `calculations.ts` engine routes (memo §3.1). *(Observed while writing this row: the ledger has no rows for the ~#262–#316 train, 2026-07-20 → 08-04 — backfill spun off as its own task rather than silently patched here.)* | **None** — docs-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change. | kb-index-guard **PASS** (105 docs, no dead links); `tsc` clean; gate green on the PR; post-merge `/api/health` probe per the section above (docs-only ⇒ no runtime delta). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-07-19 | **PR #261 — docs-only: the 2026-07-19 protection flip-gap-restore, recorded; doctrine hardened to "verify enforcement before trusting it"; this ledger row rides in the same PR.** Timeline: ~17:20Z the founder made the repo **private** (to protect the code) — on the Free plan GitHub then **deleted** the protection rule outright (API 404, not a suspension), and **#252–#259 merged pre-green** (`--auto` cannot arm with nothing required — it merges instantly, gate still running; all eight gates passed post-hoc, #259's squash tree verified == tested head). ~19:45Z the founder reversed to **public** ("for now, pro later"); a parallel session re-applied the rule from DB_MIGRATIONS' documented table and probe-verified BLOCKED via unmerged PR #262. Corpus updated to the durable doctrine — **platform enforcement follows plan/visibility; verify `gh api …/branches/main/protection` lists the gate before relying on `--auto`, else watch-then-merge** — in TEAM_PRACTICES §3/§5.4/§6 · this file (CI status/Shipping/Reverting) · PLAYBOOK step 9 · DB_MIGRATIONS (rule table = the proven restoration record) · ROLLBACK §2 · LOCAL_DEV · app-guide 01/10 · PRE_PRODUCTION_OPS · ASSUMPTIONS; security-governance claims made lapse-resilient in ACCESS_CONTROL_POLICY §5 · INFORMATION_SECURITY_POLICY §8 · Plaid Q7 (flagged accuracy note); ASSET_REGISTER GitHub row now records **public visibility** + world-readable kb + the secret-scanning/push-protection enable boxes. | **None** — docs-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change. | kb-index-guard **PASS** (103 docs, no dead links); corpus sweeps for stale protection phrasing → zero hits in living docs (immutable rows/logs/archive excepted); the gap bounded by merge-vs-gate timing (#250 merged 2s **after** its gate at 17:19Z; #252 merged ~2min **before** its gate at 17:42Z); 403/404 evidence and the restored rule both read live in-session; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-07-19 | **PR #260 — fix(checkup): production-build check un-broken + orphan-scan learns the test-lane convention; this ledger row rides in the same PR.** The first full `pnpm checkup` after the week's train came back 10/12 with both fails in the **checkers**, not the code: (1) `pnpm build --silent` — pnpm v10 forwards post-scriptname flags into the script line, so `--silent` hit esbuild (last command of the `&&` chain) and the build check failed on a **healthy** build; `--silent` dropped from the test/build checks (output is captured to `$LOG` regardless), trap documented in-script. (2) `orphan-scan.cjs` flagged the 14 colocated `*.test.{ts,tsx}` from #253–#257 as never-imported — they are vitest glob ENTRIES; excluded from orphan *candidacy* only (still reference sources, so test-only-imported modules stay flagged). Rode along: checkup `PROD_URL` default → `https://www.homiquity.com` (canonical since the 07-13 cutover). | **None** — two ops scripts; nothing in the runtime bundle or the CI gate path; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change. | Orphan scan now "No orphaned files — codebase fully connected"; `bash -n` clean; plain `pnpm build` proven green on identical code; gate green on the PR; **full `pnpm checkup` re-run on the synced primary post-merge → expected 12/12** (recorded in the session); post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (ops scripts only). |
| 2026-07-19 | **PR #259 — chore(checkup): `scripts/checkup.sh` `PROD_URL` default → `https://www.homiquity.com`** (canonical host since the 07-13 cutover; the old vercel.app domain still resolves — doctrine alignment, not breakage). **Row added retroactively in #261, same session** — the #259 session initially omitted it; the "rode along" checkup-`PROD_URL` line in #260's row above refers to this same change, which landed here. #259 is also the merge that **exposed the enforcement lapse** documented in #261's row: `gh pr merge --auto` merged it while its gate was still `in_progress`. | **None** — script-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; not in the runtime bundle. | The script's exact probe run pre-merge against the new default → `"status":"ok"`; `grep` over `scripts/` → no other old-host references; gate green post-merge on both the PR head and the `main` push; squash tree verified == tested head; `/api/health` probed 19:19Z OK. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (script-only). |
| 2026-07-19 | **PR #258 — ci: design-token ratchet promoted into the required gate; this ledger row rides in the same PR.** The last deferred intervention from the [adjudication log](../logs/2026-07-19-modular-architecture-pitch-adjudication.md) §4 (option 3), taken after #253–#257 stabilized baselines at **0 raw-palette / 97 bare-white-black**. New `pnpm guard:tokens` script + fifth `gate` step — **the job `name:` is untouched** (verbatim required-check string; rename = PR deadlock). Residual documented in-step + §Checks: the `strict:false` racing-merge window (the #112 baseline-race class) now surfaces on the **next** PR's gate instead of lingering until a manual run; fix forward by retokening or ratcheting the baseline. Docs same-PR: this file (diagram · CI-status · §Checks five-command list, guard removed from the manual list), 10-deploy-ops operator summary, ui-components skill. | **None** — CI workflow + package.json script + docs; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; nothing enters the runtime bundle. | `pnpm guard:tokens` green at baseline (0/97) locally; gate job name byte-verified against the protected required-check string pre-push; **this PR's own gate run executes the new "Design-token ratchet" step end-to-end**; post-merge `/api/health` probe per the section above (CI/docs-only ⇒ no runtime delta). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>` removes the step and restores the manual-list docs (safe; protection's required-check string never changed). |
| 2026-07-19 | **PR #257 — refactor(borrower): boy-scout split #4 (FINAL), `Dashboard.tsx` 1,092 → 471 lines; this ledger row rides in the same PR.** Completes the giant-page decomposition (#254 → #255 → #256 → this): all four 1,000+-line pages are now feature folders with characterization tests. The page's five in-file components + pure helpers relocate verbatim into `borrowerDashboard/`: **model.ts** (types · `NEXT_ACTION_ICONS` · readiness/greeting/expiry/activity-time helpers; **8 unit tests — first-ever coverage**, readiness weights pinned exhaustively over the shared `LOAN_APP_STATUSES` so a new status cannot score silently), **FinancialSnapshot**, **CollapsibleActivity**, **LoanDetails** (3 tests incl. the expired-expiry suppression rule), **PreQualLetterCard**. | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; no new dependencies. | `tsc` clean; `pnpm test` **1,356 node + 61 client green** (13 new); token guard **0/97 at baseline**; **live-driven on :5002 as buyer@test.com** (real pipeline data): extracted greeting renders the doc-collection subtitle, next-action card w/ icon map ("Upload your documents — 7 needed"), all three collapsible sections live in the DOM (details ×2 · activity ×5 · snapshot); gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client-only). |
| 2026-07-19 | **PR #256 — refactor(funnel): boy-scout split #3, `PreApproval.tsx` 1,371 → 906 lines; this ledger row rides in the same PR.** Deliberately conservative for the conversion-critical `/apply` funnel: **render-layer extractions only** — machine/gates/autosave/draft-restore/submit untouched; small form-entangled switch cases stay inline. Into `preApproval/`: **StateStep** (the licensed-state gate, mirrors the server 422; 3 tests pinned to the shared footprint — unlicensed shows shared `unlicensedStateMessage` + holds, licensed advances), **IncomeSourcesStep** (controlled on purpose — draft-restore's `applyIncomeSources` reseeds parent-owned stores; 5 tests incl. rental `annualAmount` derived Σ monthly×12), **FunnelChrome** (RestoreDraftBanner #249 one-decision-point · AuthGateOverlay · FunnelFooter; 3 tests incl. regulatory copy pins — soft-inquiry, not-a-commitment, broker disclosure, Equal Housing). | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; no new dependencies. | `tsc` clean; `pnpm test` **1,356 node + 48 client green** (11 new); token guard **0/97 at baseline**; **live-driven on :5002** per the funnel recipe (step-counter assertions): extracted footer on every step; seeded draft ⇒ **RestoreDraftBanner appeared, Restore hydrated form+step** ("Progress restored", advisory computed DTI 29% / $2,413/mo from restored answers, landed Step 6); **Wisconsin ⇒ live unlicensed notice (Illinois-only, NMLS #427468) + held at Step 6; Illinois ⇒ advanced to Step 7**; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client-only). |
| 2026-07-19 | **PR #255 — refactor(staff): boy-scout split #2, `StaffDashboard.tsx` 1,408 → 602 lines; this ledger row rides in the same PR.** Same recipe as #254 (row below). Four verbatim extractions into `staffDashboard/`: **CreateTaskDialog** (task-engine creation surface; 4 tests pin doc-request conditionality + priority/category/year pickers to the shared model lists; application select stays parent-controlled for the Review tab's Add-Task preselection), **MyQueueTab** (role-scoped SLA queue; 2 tests), **ReviewTab** (two-axis verdicts: verify ⇒ COMPLETED/verified, reject ⇒ OPEN/rejected; owns `updateTaskMutation`; 4 tests incl. onAddTask handoff + role-honest headings), **ComplianceTab** (GSE/ULDD/QM readiness · ECOA §1002.9 AA counters · FCRA/ECOA/GLBA retention report — owns the retention queries, now firing on tab mount not page load · audit/automation panels; 2 render tests incl. AA escalation copy). Type surfacing: `getUserName` prop typed `string \| null` (`User.email` nullable). | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; no new dependencies. | `tsc` clean; `pnpm test` **1,356 node + 37 client green** (12 new); token guard **0/97 at baseline**; **live-driven on :5002 with three role fixtures** — admin: Pipeline w/ 143 real loans + CreateTaskDialog in doc-request mode; underwriter: default-lands on ReviewTab (empty state + "Team Applications"); processor: default-lands on MyQueueTab rendering **263 real SLA-sorted rows**; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client-only). |
| 2026-07-19 | **PR #254 — refactor(staff): boy-scout split #1, `BorrowerFile.tsx` 1,432 → 567 lines; this ledger row rides in the same PR.** First page of the giant-page decomposition ([adjudication log](../logs/2026-07-19-modular-architecture-pitch-adjudication.md) §4 #2, founder-approved). Four verbatim extractions into `borrowerFile/`: **StatusUpdateDialog** (the #247 phantom-status home — 5 characterization tests pin options == `STAFF_SETTABLE_STATUSES` exactly, credit-decision locks, ≥2 HMDA denial reasons, approval-outcome-needs-verified-financials), **ConditionsTab** (+ action dialog; 4 tests incl. the waive/N-A written-reason audit rule), **CreditTab** (FCRA consent · tri-merge pull · ECOA/Reg B §1002.9 postal-fallback delivery · audit log; owns its queries — one deliberate delta: credit queries now fire on tab mount, not page load), **TimelineTab** (2 tests). Guard safety pre-checked: `statusVocabulary.test.ts` walks all of client/src (family-scoped), so the split cannot dodge it. | **None** — client-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; no new dependencies. | `tsc` clean; `pnpm test` **1,356 node + 25 client green** (11 new characterization tests); token guard **0/97 at baseline**; **live-driven on the worktree server** (:5002, admin fixture, real file): status dialog full vocabulary + selection, 7 real outstanding conditions w/ actions, real timeline, Consent Active + Pull Credit armed, documents tab regression-checked, `?tab=` deep-links throughout; gate green on the PR; post-merge `/api/health` probe per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (client-only). |
| 2026-07-19 | **PR #253 — test(client): component-test lane; this ledger row rides in the same PR.** Builds intervention #1 of the [2026-07-19 adjudication log](../logs/2026-07-19-modular-architecture-pitch-adjudication.md) §4 (founder-approved): UI verification was 100% manual — 2,586 `data-testid` hooks, zero component-test consumers. New `vitest.client.config.ts` (happy-dom + react plugin, **glob** include `client/src/**/*.test.{ts,tsx}` so colocated tests can't be stranded); **`pnpm test` now runs node + client suites ⇒ the required gate catches UI regressions**; `pnpm test:client` runs the lane alone. Seed tests (14): `DocumentStatusBadge` vocabulary pin incl. the **Reg N** never-reads-as-a-loan-decision rule; `ui/button` user-event interaction + disabled contract. 4 devDependencies (@testing-library/react 16 + dom + user-event, happy-dom) — none enter the runtime graph. Docs same-PR: TEAM_PRACTICES §5.2/§5.4 (component-test-first for UI behavior), this file's §Checks, ui-components skill. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; devDeps don't ship in the serverless bundle (`--prod` tree unchanged, audit clean). | `tsc` clean (`*.test.tsx` type-checked — not tsconfig-excluded); `pnpm test` **1356 node + 14 client green** (the gate's exact command); design-token-guard **0/97 at baseline**; `pnpm audit --prod` clean; kb-index-guard 103 docs OK; gate green on the PR; post-merge `/api/health` probe per the section above (test-infra-only ⇒ no runtime delta). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (tests/config/docs; reverting also unwires the client suite from `pnpm test`). |
| 2026-07-19 | **PR #251 — chore: retire dead/forbidden `package.json` scripts; this ledger row rides in the same PR.** `save`/`sync` **removed** — both direct-push `main`, dead since the 2026-07-17 branch protection (#250 scrubbed the last doc references). `db:generate`/`db:push` **replaced with failing echo stubs** (print the house rule, exit 1): generate has snapshot drift in this repo, push drops other branches' columns on the shared dev DB and prod is migrate-only — the stub meets Drizzle muscle memory with the doctrine instead of a bare "Missing script" that invites `npx drizzle-kit push` directly. Zero consumers repo-wide (`scripts/checkup.sh`, `.claude/launch.json`, `.github/workflows/` all grep-clean); `db:migrate`/`db:migrate:prod`/`db:migrate:adopt` untouched. | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change; package.json scripts are not part of `vercel-build` or the runtime bundle. | `package.json` parses (`node -e JSON.parse`); stubs verified live (`npm run db:push`/`db:generate` → doctrine + exit 1; `save`/`sync` → `Missing script`); `tsc` clean + unit suite **1356 green** (102 files); gate green on the PR; post-merge `/api/health` probe per the section above (scripts-only ⇒ no runtime delta even if the Vercel quota queues the build). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (scripts-only, no runtime path). |
| 2026-07-19 | **PR #250 — docs-only: rule-corpus conflict reconciliation (18 `.md` files); this ledger row rides in the same PR.** Three researched conflict families resolved to the live enforcement layer: **(A)** the retired founder-supervised raw-`pg` prod-migration recipe + local `PROD_DATABASE_URL` scrubbed from TEAM_PRACTICES traps, PLAYBOOK §3, app-guide 03/06/10, ROLLBACK §3, COMPLIANCE_COUNSEL_REVIEW §0, BETA_GO_LIVE, PROD_ACCEPTANCE — all now state the `migrate-prod` auto-apply pipeline; **(B)** pre-protection push doctrine (founder-approved direct pushes, `--no-ff` integration batches, `pnpm save`/`sync` as daily driver, 10-deploy-ops' "no CI gates"/"removed by choice", routines' "publish local main") rewritten to the protected PR lane in TEAM_PRACTICES §3/§6, PLAYBOOK rule 1 + step 9, ROLLBACK §2/§4, LOCAL_DEV, 01-start-here, 10-deploy-ops, PRE_PRODUCTION_OPS + this file's revert line; **(C)** the "commit both lockfiles via `pnpm import`" instruction (would resurrect the poisoned `package-lock.json`) replaced with pnpm-only in TEAM_PRACTICES + 10-deploy-ops. Stale facts corrected: ASSUMPTIONS licensure row (NMLS **#427468**, IL footprint) + CI row (gate live), SAFE_MLO NMLS-display item, PLAYBOOK §4 licensing row + `storage/` tree line, README host/status, this file's env-var list (`ANTHROPIC_API_KEY` in, Gemini/OpenAI retired), calculators count. New research-grounded rules: journal-`when` uniqueness (the `appliedWhens` silent-skip in `migrate-prod.cjs`), post-deploy health probe wired into TEAM_PRACTICES §6, `strict:false` racing-squash-merge trap. Immutable ledger rows + archive/logs untouched. | **None** — docs-only; no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0037`**); no env-var change. | kb-index-guard **PASS** (102 docs, all indexed, no dead links); repeated corpus sweeps for every stale pattern → zero hits in living docs; facts re-verified against source, not memory: live `gh api …/branches/main/protection` (gate required, `enforce_admins: true`), `ci.yml` + `migrate-prod.cjs` read, `.env.example`, `shared/companyIdentity.ts` (427468 / `["IL"]`), both prod hosts probed 200. Gate green on the PR; docs-only ⇒ no runtime risk; `/api/health` probed post-merge per the section above. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-07-17 | **PR #226 — docs-only: this file's header un-stale'd to the live PR-flow + required-gate reality; this ledger row rides in the same PR.** The header still opened with "there is no CI on `main` today" (corrected 2026-07-04) and the checks section still framed CI as "a deliberate future choice" — both predate #176's branch protection and contradicted the rest of this file. Rewrote the header/diagram (PR → required `gate`: typecheck · unit tests · blocking prod audit · schema guard → merge → Vercel deploy + `migrate-prod` auto-apply), Shipping (the `npm run save`/`npm run sync` direct-push recipe is dead — `enforce_admins` rejects pushes to `main`), and "Optional checks" → "Checks — what the gate enforces, and what stays manual" (integration tests + design-token guard remain manual, never in CI). Post-deploy health-check section, `pnpm.overrides` rules, and all prior ledger rows untouched. | **None** — docs-only (one `.md`); no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0032`**); no env-var change. | Facts re-verified against source, not memory: `ci.yml` read on this branch (4 gate steps; migrate-prod trigger + journal-only dry-run) + live `gh api …/branches/main/protection` (required check `gate (typecheck · tests · schema guard)`, `enforce_admins: true`, no required reviews, force-push/deletion of `main` blocked); link targets exist + no inbound links to the renamed anchor (corpus grep); gate green on the PR. ⚠️ Merged inside the Vercel build-quota freeze — this merge's deploy queues unbuilt; prod stays frozen HEALTHY at #222 (docs-only, no runtime risk; probe `/api/health` when builds resume per the section above). | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-07-17 | **Prod API outage (~15:03–15:19Z, ≈16 min) + forward fix — PR #222 (`b87d830`).** #219 (`4eb456a`) moved the overrides block to `pnpm.overrides` so it finally applied; the uuid override from the `524335b` vulnerability sweep was a **floor** (`">=11.1.1"`), which resolved to **ESM-only uuid@14** and broke `gaxios`'s CJS `require("uuid")` in the Vercel function loader — every `/api/*` route returned the `bootError` JSON behind READY, aliased deploys while the static front end kept serving. Fix: cap the override at `^11.1.1` (same security floor, newest major with a real CJS build); 2-file diff (`package.json` + 7 `pnpm-lock.yaml` hunks). Full narrative + binding lessons: [postmortem](../logs/2026-07-17-prod-api-outage-uuid-esm-postmortem.md); the new "Post-deploy health check" section above is this incident's enforcement. *(This ledger row + postmortem land in a docs-only follow-up PR, same day.)* | **None** — no `migrations/` or `shared/schema/` change (prod migration HEAD stays **`0032`**); no env-var change. Dependency graph only. | Root cause pinned by micro-repro (`createRequire(<gaxios pkg>)("uuid")`: before → `uuid@14/dist-node` ESM, the exact module in prod's error; after → `uuid@11.1.1/dist/cjs`, loads); built server boots locally with `/api/health` ok; `pnpm audit --prod` clean (floor preserved); gate green. **Prod recovery verified live: `GET /api/health` → `{"status":"ok"}` at 15:19:34Z + `/api/auth/providers` serving real data.** Broken window bounded by Vercel API evidence (#221 deploy `dpl_WwBpC9XgjiDCW2vts4fzhKrnejqL` READY/aliased yet API-dead, probe-verified). | **Do not `git revert b87d830` — reverting re-breaks the entire prod API** (restores the uuid floor). Forward-fix only; if uuid must move majors, prove `exports.node.require` exists first ([postmortem §7](../logs/2026-07-17-prod-api-outage-uuid-esm-postmortem.md)). |
| 2026-07-17 | **Borrower document portal — 3 PRs, squash-merged in order #208 → #212 → #213** (one session; built from a Better/Blend design brief, founder-approved full scope). **#208** (`51ea772`) closed the document review loop: `shared/documentStatus.ts` status vocabulary + `DOCUMENT_REVIEW_ROLES`; **migration `0032`** (`rejection_reason`, `reviewed_by_user_id`, `reviewed_at` on `documents`, expand-only idempotent); `POST /api/documents/:id/verify` now persists the decision, reverts no-longer-satisfied submitted conditions (`revertConditionsForRejectedDocument`), and notifies the borrower — in-app carries the reason (stays behind login), **email is content-free by doctrine** (reason/filename never travel over email, test-pinned); staff Accept/Reject UI (superseded same-day by #211's workbench, which converged on the same endpoint); canonical `DocumentStatusBadge` (borrower vocabulary Under Review / Accepted / Action Needed — never "Approved", test-pinned) fixing the dead `pending_review` badge. **#212** (`4b70f51`) dropzone UX: hand-rolled `DocumentDropzone` (idle/drag-active/drop-error, semantic tokens), real byte-level XHR progress + working cancel in `use-upload.ts`, `shared/uploads.ts` becomes the single accepted-type source (server allow-list re-exports it), 25MB→10MB copy fix. **#213** (`f5281c3`) personalized checklist: pure `buildDocumentChecklist` derives the borrower checklist from the pipeline's `loan_conditions` (alias-aware matching, latest-upload-wins, `verifying` passthrough, rejection reason exposure; standard-list fallback), route rewrite, one `DocumentItemRow` rendering both paths. | **Migration `0032` auto-applied to prod on the #208 merge — `migrate-prod` job green** (journal idx 32, `when` 1784565600000, monotonic). No env-var changes. | Per-PR gates green (typecheck · unit suite grown 1212→1266 across the train · schema guard · token guard at 0/97 baselines); §9 security-review notes recorded in the #208 (outbound messaging) and #212 (uploads) PR bodies. **Browser-verified end-to-end on the worktree dev server**: drag states + type/size rejection → upload → Under Review → staff reject-with-reason → borrower Action Needed + in-app notification + content-free email (console transport) → re-upload re-arms the condition → staff accept → Accepted; personalized mode verified against seeded pipeline-style conditions (alias bridge matching live), `?condition=` focus mode, no-application fallback, Messages dialog regression. ⚠️ Deploy caveat: these merges landed inside the Vercel **build-rate-limit freeze** — their deploys never built; the code reached prod via the post-quota deploy chain, which was then **API-dead until #222** (see the row above). First healthy prod build containing the portal = #222's. | [ROLLBACK.md](./ROLLBACK.md) §1; isolated **squash** commits, revert newest first: #213 `f5281c3`, #212 `4b70f51`, #208 `51ea772`. Migration `0032` is additive — safe to leave in place on a code revert (no automatic down). |
| 2026-07-12 | **Docs housekeeping — PR #141 (docs-only); this ledger row rides in the same PR.** Brings the governance record in line with the now-deployed money-path train: adds the row immediately below (#135→#139), flips `BETA_GO_LIVE_READINESS.md` §2 from "awaiting merge" to "merged & deployed", and adds a 2026-07-12 `CTO_ROADMAP` status box (also filling the gap the closed #128 roadmap-refresh left). | **None** — docs-only (`CTO_ROADMAP.md` + two runbooks, `.md` only); no `migrations/` or `shared/schema/` change (migration HEAD stays `0023`); no env-var change. | Edits to already-indexed docs (KB index unaffected); docs-only Vercel build — prod deploy verified **READY** with `GET /api/health` 200 after merge. | [ROLLBACK.md](./ROLLBACK.md) §1; single **squash** commit — `git revert <sha>`, safe (docs only). |
| 2026-07-12 | **07-12 money-path merge train — 5 PRs, founder-merged in order (#135 → #136 → #138 → #137 → #139), all squash commits** (the gated-beta money-path session). **#135** (`c7260de`) money path for the gated live beta: new `storage.assignLoanOfficer()` intake→LO handoff chokepoint (keeps `loanOfficerId` + the primary `loan_officer` deal-team row in lockstep, idempotent) + intake **pool/claim** (`GET /api/pipeline/unassigned`, `POST /api/loan-applications/:id/claim` lo/loa with a 409 steal-guard) + auto-route referred applicants at intake; **Run DU/LPA** button wired into `SubmissionReadinessDialog` and gated through `getLoanApplicationWithAccess` (**closes an AUS IDOR** on `POST /api/underwrite/submit-gse`); `verifyInternalStaffApplicationAccess` widened to `loanOfficerId` **OR** active deal-team membership (a file in the LO's own queue no longer 403'd in the cockpit); XSD conformance recorded **non-blocking** on each `lender_submissions.readinessSnapshot.xsdConformance`; re-wired the "Submit to lender" terminal action the LO-1 cockpit rewrite (#133) had silently dropped; demo seed `server/scripts/seedDemoFile.ts` (dev/shared-DB only). **§9 security-reviewed — net-positive.** **#136** (`f5023c1`) F-007: `passwordHash` stripped from the `/admin/users` response; F-002: `recordStageTimestamp` + `updateConditionMetrics` writers wired at the intake/pipeline/decision/condition seams so the `loanOutcomes` + dashboard tables stop rendering off empty data. **#138** (`db20b8a`) Reg N §1014.3(q) **hard-block** on LO "approval guaranteed" outbound comms (clause-scoped negation guard; citation fix `§1014.3(i)`→`(q)`); one-shot scrub script removed the single existing prohibited `team_messages` row. Compliance-auditor validated. **#137** (`c88d76b`) UDAAP-safe borrower predictive insights — replaced the bold closing-odds `%` + single "days to funding" integer with a **qualitative outlook** + typical **week-range** + a conspicuous non-binding disclaimer (`PREDICTION_INSIGHTS_DISCLAIMER`, 12 U.S.C. §5531(d)/§5536). Compliance-auditor validated. **#139** (`de088fe`) beta go-live readiness snapshot doc (feeds `PROD_ACCEPTANCE_TEST`). | **None** — no commit in this train touches `migrations/` or `shared/schema/`; migration HEAD on `main` remains **`0023`** (journal `0000`–`0023`, 24 files). No env-var changes; no new `.env.example` keys (vendor legs stay deterministic simulations behind adapters). | Per-PR gates recorded on each PR (not re-run here): #135 money-path integration + full unit suite green, **§9 security review clean** (net-positive — closes the AUS IDOR), driven live on `:5002` incl. the terminal submit → hashed staff-only MISMO 3.4 package; #137 + #138 **compliance-auditor validated** (the auditor caught + I fixed a negation-guard gap and the `§1014.3(i)`→`(q)` cite pre-merge); #136 `tsc` clean. Prod deploys verified via the Vercel API: **current prod = #139 `dpl_ApJzgcLUuf2mJZUTG2hoHBeBVC19` READY**, `GET /api/health` **200**; #135 `dpl_5CEDUBpDoePQ8nsgeQUwSBQQ3tvL` **READY**; #136 `dpl_HEjwhx7G9gdTgokajv1tBTBMNZWa` **READY**. ⚠️ **#138 `dpl_DGeG9cD8WB7E7AuDWgB6QrmcLyxr` and #137 `dpl_HiQztPcZL8qNEsaZASEjnx3RDKej` auto-CANCELED** — superseded within seconds by the next merge in the rapid-succession train (the four post-#135 merges landed 17:52:41–52, seconds apart); both fixes reached prod via #139's deploy, which contains them (same supersession pattern as #111 two rows below). | [ROLLBACK.md](./ROLLBACK.md) §1; each is an isolated **squash** commit — `git revert <sha>` (no `-m 1`), newest first: #139 `de088fe`, #137 `c88d76b`, #138 `db20b8a`, #136 `f5023c1`, #135 `c7260de`. No migration to roll back. |
| 2026-07-11 | **07-11 close-out batch — 9 deploys, one folded row** (newest→oldest, all squash commits): **#117** lender-deck ask slide finalized with founder contact (`538a27b`, docs/demo) · **#118** footer divider → the sidebar surface's dedicated `border-sidebar-border` hairline token, follow-up to #112 (`883202e`) · **#116** `/admin/users` renders CPA users instead of blanking — `ROLE_CONFIG` had no `cpa` entry so three render sites threw on `undefined.icon`; added the entry + a fail-soft `getRoleConfig()` (`1455dcc`) · **#113** lender demo kit: editable 17-slide deck, PDF, video script + real product screenshots (`a86f6e7`, docs/demo) · **#111** staff borrower-file LTV/DTI display fix — double-scaling showed LTV 9000%/DTI 3625%; only render site with the bug (`2a5cc26`) · **#112** unbroke the design-token guard on `main`: #95's 123 baseline was stale-on-arrival (#97 landed `hover:text-white` pre-merge — a baseline race, not a regression); Footer's 27 white literals retokened onto `sidebar-foreground`, baseline ratcheted **123 → 97** (`b91f15a`) · **#110** COI PartnerHub Program charter + 6 build prompts w/ binding §5 compliance corrections (`4491ec5`, docs/specs) · **#109** LO Advisor Program charter + 6 build prompts (`c014e64`, docs/specs) · **#100** IL DPA sitemap test re-pointed at the DB-driven generator #91 introduced — was failing on every clean checkout (`cbab90e`, tests-only). *(Batch row logged same-day by a ledger catch-up session from PR/merge/Vercel evidence — TEAM_PRACTICES §6 was missed by the originating sessions; CTO_ROADMAP's 07-11 status box flagged the gap.)* | **None** — none of the nine touch `migrations/` or `shared/schema/`; migration HEAD on `main` remains **`0019`** (see the income-engine row below). No env-var changes. | Per-PR gates (recorded on each PR, not re-run here): `tsc` clean across the code PRs; #112 **883/883 unit** + guard `ratcheted down 123 -> 97 ✅`; #116 live-verified (renders all 44 users incl. 13 CPA rows); #111 live-verified on a seeded file (LTV 90.0%/DTI 36.3%); #118 hairline live-verified (`rgb(52,71,121)` @ 1px, guard 97 == baseline). Prod deploys verified via the Vercel API: current prod = #117 `dpl_24HVkYxM4tK2XkEwGMa6b8p5ZQMY` **READY**; #118 `dpl_AurJjjVSJZ18RbWQzYauMA61yson`, #116 `dpl_CToNHAtJrz4G1jqbnb9i8AgN1C4f`, #113 `dpl_9jY6xh77KQWgKF1kvR73VHN4EhAt`, #112 `dpl_BBN2VJcrJ3WbxZSE84AsE7Nj13fu`, #110 `dpl_EskLEEx4C3pf3EeXaBfYduuC8txs`, #109 `dpl_A8Kbrnsic2XQznZGUCdtz4LwmDth`, #100 `dpl_DyECpewsKAgQE2RWSvZgjJBVD3Tb` all **READY**; ⚠️ **#111's own deploy `dpl_6A5vhvdXjJqq5qbr2eyg9zeWy6RM` was auto-CANCELED** (superseded 3s later by #113's push, which contains it) — #111's fix reached prod via the #113 deploy. | [ROLLBACK.md](./ROLLBACK.md) §1; each is an isolated **squash** commit — `git revert <sha>` (no `-m 1`). |
| 2026-07-11 | **Complex-Borrower Income Engine — UAL program P1–P6 (PRs #103 + #108, 2 deploys)**. **#103** (squash `f41810d`) UAL P1: deterministic Fannie Form-1084 self-employment income — `computeSelfEmploymentQualifyingIncome()` (Schedule C add-backs; K-1 gated on documented distributions or the B3-3.6-07 liquidity ratio; 2-year averaging with a conservative decline/loss guard that sets `requiresManualReview`), replacing the `$0` SE-income skip in `qualifyIncome`; cites the verified B3-3.5/B3-3.6 reference landed in `docs/fannie-mae/` (which caught Fannie's 03/04/2026 chapter reorg). **#108** (squash `322ec2d`) P2–P6 in one integration: **P2** Situation Identification Engine — two-pass multi-form tax extraction + deterministic entity resolution + cross-form tie-outs citing the official IRS PDFs in `docs/irs-forms/`, SituationProfile classifier + staff Tax Intel tab; **P3** single deterministic multi-path income orchestrator (append-only `income_path_evaluations`, SHA-256 fingerprints, wage-earner byte-parity guard on the `qualifyIncome` cutover); **P4** Angel Oak non-QM program authority (`docs/lender-programs/angel-oak/`) flipping cited DSCR + bank-statement calculators on (ratio-only, never pass/fail); **P5** accuracy loop — provenance-tracked smart-fill (confirm-only writes) + exception-only review workbench; **P6** income analysis package on `lender_submissions` (hash-manifested, per-lender shaping, stage-3 readiness gate). Two horizontal-IDOR security fixes landed in-stack pre-merge (deal-team scoping on tax-intelligence staff reads; review-item income-path sync scoped to the owning application). *(Row logged same-day by a ledger catch-up session.)* | **Migrations `0013`–`0019` applied to prod 2026-07-11**: `0013_self_employment_income` (#103) + `0014_tax_intelligence`, `0015_borrower_business_entities`, `0016_situation_profiles`, `0017_income_path_evaluations`, `0018_income_review_workbench`, `0019_income_analysis_package` (#108) — prior-session record: raw `pg` client, per-migration transaction + manual `drizzle.__drizzle_migrations` journal insert (Neon-pooler recipe, [TEAM_PRACTICES](../governance/TEAM_PRACTICES.md) §5). ✅ **Independently re-verified read-only 2026-07-11 (this row's session): prod `drizzle."__drizzle_migrations"` holds 20/20 rows (0000–0019) and the newest 8 hashes byte-match the local sha256 of `0012`–`0019` — prod migration HEAD = `0019`.** All additive (new tables/columns). No env vars added to `.env.example`; vendor legs stay deterministic simulations behind adapters. | #103: `tsc` clean, **768 unit** green (incl. the 227-line `selfEmploymentIncome.test.ts`); rebased onto latest `main` pre-merge. #108: `tsc` clean on the collapsed tree, **883 unit** green; every prompt runtime-verified on the dev DB during its build (extraction → entities → tie-outs → SituationProfile; orchestrator parity cutover; DSCR ratio; full accuracy loop incl. a confirmed 1084 figure; per-lender package with no PII leak; readiness gate). Prod deploys **READY** (Vercel API): #103 `dpl_2BWenzFV3nEPAWzbPT7rM54VtaiA`, #108 `dpl_Boct8Ni3X9fuijSrMonM3eSMgR1K`. | [ROLLBACK.md](./ROLLBACK.md) §1, §3 — `git revert 322ec2d f41810d` (both squash commits, newest first). Migrations have no automatic down; the additive tables/columns are safe to leave in place on a code revert — full schema rollback = Neon snapshot/branch restore per §3. |
| 2026-07-08 (eve) → 07-09 *(retroactive — logged 2026-07-11)* | **Marketing/UI/partner wave — 9 PRs** (merge order): **#92** contained veteran-founded trust mark — `VeteranFoundedBadge`, scoped off-palette `--veteran-*` vars, factual copy, no red/white/blue repaint (merge `1dd1c30`) · **#94** homeowner lifestyle photography — photo-beside-text heroes + build-out rows via `FramedPhoto`/`ImageTextSection`, one-line swaps via the `lifestyleImages` manifest (merge `2c805c7`) · **#93** Royal Blue Emerald repaint (dark chrome charcoal → vivid royal blue) + shared `StatCard`/`EmptyState`/`QueryBoundary` primitives + a11y/mobile fixes (squash `3a6cecc`) · **#96** consumer attribution fixes (LO `/ref` + CPA `/cpa` post-auth replay via `usePendingAttribution`; `/apply` funnel un-stranded) + the `/partners` partner/COI waitlist → **new `partner_waitlist` table, migration `0012`** (squash `5d2ff1f`) · **#97** footer "Partner with us" link (squash `2d67ed9`) · **#95** QueryBoundary/`QueryErrorState` honest error+retry rollout across 11 data pages (squash `424fa06`) · **#98** `/admin/partners` waitlist admin view (squash `10bc9e7`) · **#91** SEO foundation + blog content engine Phases 0–5: per-page metadata/social cards, JSON-LD, bot-UA server-side head injection, DB-driven `/sitemap.xml`, dependency-free markdown renderer, evergreen starter cluster, glossary single-source + the bare-`/api` `vercel.json` rewrite fix (squash `513977b`) · **#99** LO Rate-Lock Desk: orphaned `rate_locks` backend wired into the LO Command Center + 2 latent `extended`-status bugs fixed via `inArray`, expiring route deal-team-scoped, new rate-lock-alerts cron entry in `vercel.json` (squash `bf0cca6`). | **Migration `0012_partner_waitlist`** (new B2B interest-capture table, via #96) — flagged at merge as applied-to-dev-only; prior-session record has it applied to prod via the §5 raw-pg recipe. ✅ **Presence on prod confirmed read-only 2026-07-11: `0012`'s hash byte-matches row 13/20 in `drizzle."__drizzle_migrations"`** (the journal stores folderMillis, not apply time, so the exact apply date isn't recoverable — presence is). No env-var changes (#99's cron sweep authenticates via the pre-existing `CRON_SECRET`). | Per-PR gates (recorded on each PR): #96 typecheck clean + **754 unit** + security review clean (Zod-validated, parameterized, honeypot + rate-limited, no enumeration); #99 `tsc`+guard clean, **751/751 unit + 97/97 integration** (new `rateLocks.test.ts`) against a live server, runtime smoke (401/200 matrix on `/api/rate-locks/expiring` + `/api/jobs/rate-lock-alerts`), security review clean (net-positive scoping); #91 verified on its Vercel **preview deploy** (bot-UA head injection + `/sitemap.xml`; the rewrite fall-through was caught + fixed pre-merge); #98 endpoint driven live (anon 401 / admin 200); #92/#93/#94 `tsc` + design-token guard green per PR (the #95↔#97 baseline race that later broke the guard on `main` is root-caused + fixed in #112, two rows above). All 9 production deploys **READY** on `mortgage-stream.vercel.app` (verified via the Vercel API): #99 (wave head) `dpl_Hqoxfgxf4Bqkzzk27s9KX7SdJsW7`, #91 `dpl_EsCiLdtAUHUM5qPpccTYiJ38NxrR`, #98 `dpl_5VmbMCzsLLWZBzeXBS9aFJcnX3CK`, #95 `dpl_nHRJ3TVbzeaWiENZXyA3PmxEcMnF`, #97 `dpl_JBdsbq3LWoALyzKWTvP4BqAEwz8M`, #96 `dpl_3x8Es7LrdSiHcdHPHQ6UFPbpNTJA`, #93 `dpl_2Dhcj3SD1euWvMEACqhiHBMgVdrS`, #94 `dpl_HfXUinwJcLToTNcJ1RETKQqNrGDJ`, #92 `dpl_5mRKycUukxxgV5f8dKCfJquRamr4`. | [ROLLBACK.md](./ROLLBACK.md) §1; **#92/#94 are true merge commits** — `git revert -m 1 1dd1c30` / `git revert -m 1 2c805c7`; the other seven are squash commits — `git revert <sha>`. `0012` per §3 (additive table; safe to leave on a code revert). |
| 2026-07-08 (evening) *(retroactive — logged 2026-07-11)* | **Direct-to-main chore batch — 4 deploys, no PR** (interleaved with the wave above): **`f10e2fb`** OPT-9 anonymized-data pipeline wired to a weekly Vercel cron — new `GET /api/jobs/aggregate-data` (CRON_SECRET bearer OR admin session + audit log, the established dual-trigger pattern) + a Mon 11:17 cron entry in `vercel.json`; internal-only PII-hashed aggregates, no outbound (not a §9 outbound-messaging change) · **`b4bc77c`** CH-7 pass 1: 10 dead dependencies removed (Replit-Auth-era; `pnpm-lock.yaml` pruned −254/+0, zero unrelated bumps) · **`dca1307`** CH-7 pass 2: final 5 orphaned deps — CH-7 complete, 15 total · **`1661c95`** CH-1: standardize on pnpm — deleted the proxy-poisoned `package-lock.json` (53 packages resolved against the dead `package-firewall.replit.local` proxy); `pnpm-lock.yaml` is now the single lockfile (already what Vercel builds with), `packageManager` corepack-pinned, docs/scripts/`launch.json` migrated `npm → pnpm`. | **None** — no `migrations/` or `shared/schema/` change (migration HEAD was `0011` at this point in history); no env-var changes (`CRON_SECRET` pre-existing). `vercel.json` gains the weekly OPT-9 cron (platform config that ships with the repo). | Per commit (recorded in the messages, not re-run): OPT-9 `tsc` clean + booted-server smoke (`/api/health` 200; endpoint 401 unauth + 401 bad bearer, matching `/api/jobs/lifecycle`); CH-7 both passes and CH-1 verified in an isolated worktree with corepack-pinned `pnpm@10.34.4` — `pnpm install --frozen-lockfile` exit 0 + `pnpm run vercel-build` exit 0. The install/build risk is directly proven by all four production deploys building **READY** (Vercel API): `f10e2fb` `dpl_CSXMNU3DkNqKDPKCLQ8G3vcn1fdd`, `b4bc77c` `dpl_5crBnJJLjrKCtGuQd9KoGUmU9vX8`, `dca1307` `dpl_DwsrUL7L6MnsKQP6BhfJnw6TapC3`, `1661c95` `dpl_9AsavNC6KDh6hH32eNtaCxh8k38R`. | [ROLLBACK.md](./ROLLBACK.md) §1; `git revert <sha>` each. ⚠️ Reverting `1661c95` resurrects the poisoned `package-lock.json` — prefer a forward fix for lockfile issues (see the pnpm-only lockfile note in "How the Vercel deploy works" above). |
| 2026-07-08 | **PR #90 — CPA partner nav + channel hygiene** (merge `454a41b`, fix `da7432a`). *(Row stubbed on #90's behalf by a concurrent session — from the merge + commit evidence + Vercel; owner: verify/amend.)* Routes `isPartnerRole("cpa")` to a CPA-only nav on desktop (`app-sidebar.tsx`) **and** mobile (`MobileBottomNav.tsx`) instead of the borrower nav a CPA would otherwise inherit; Footer "For CPAs" link → `/for-cpas`; corrected the stale/dangerous `shared/schema/cpaPartners.ts` docstring (cpa is a self-registering **PARTNER_ROLE, NOT a STAFF_ROLE** — the misconception behind the caught privilege-escalation bug `ae06fd4`); removed dead `getCpaFirmForUser` from `storage.ts`; added tests for `checkTaxReturnConsistency` capping (`extractionService`) + document-confidence tiering + test-seam exports. | **None** — the `cpaPartners.ts` edit is **comment-only** (no column added); no `migrations/` change. Migration HEAD on `main` remains `0011`. | Per the PR commit (not independently re-run): `npm run check` clean; **751 unit + 20 CPA/tax integration** green; live-drove the CPA login (desktop sidebar + mobile bar show the CPA nav with no borrower items; footer → `/for-cpas`). Prod deploy `dpl_H3KMr1K5dC5YgzMJJfmvwkJ4HcoY` **READY** on `mortgage-stream.vercel.app` (verified). Security-relevant (reinforces the `cpa` PARTNER_ROLE vs STAFF_ROLE boundary, cf. `ae06fd4`) — nav-routing + comment only, no new auth surface. | [ROLLBACK.md](./ROLLBACK.md) §1; `git revert -m 1 454a41b`. |
| 2026-07-08 | **Docs / skills / config cleanup — direct-to-`main` batch** (founder-authorized, no PR; branch `claude/project-markdown-audit-47f6eb`, since deleted). 3 deploys, newest→oldest: **`c504f1a`** — removed dead AI-gateway env vars (`AI_GATEWAY_PROVIDER`/`ANTHROPIC_API_KEY`) from `.env.example`, kept the live `AI_INTEGRATIONS_*` (read by `extractionService.ts`/`coachingService.ts`); **`c30758a`** — enriched the `.claude/skills/mortgage-calculations` router for the merged public calculator suite (hub + 10 pages, BUILD-1 pre-license note); **`6c6fc77`** (merge) folding **`78ecf80`** bloat pass — pruned 2 superseded founder-routine logs + purged dead `aiGateway.ts`/`documentEngine.ts` refs across app-guide 01/05/06/09 (`extractionService.ts` owns Gemini) — **`bba03b7`** banner-mode archival of the launch-era log chain (`kb/logs/{founder-routines,lo-audit,assessments}` → `kb/archive/`, ~65K tok quarantined; founder-authorized [TEAM_PRACTICES](../governance/TEAM_PRACTICES.md) §2 override; live status-vocabulary prod-migration reminder migrated out to [CTO_ROADMAP](../../CTO_ROADMAP.md) **CH-8**) — and **`ac1f12f`** four on-demand `.claude/skills/` router skills (api-routes / ui-components / mortgage-calculations / seo-content) + CLAUDE.md pointer + `app-guide/07-frontend` palette drift fix (→ Charcoal Emerald). | **None** — docs / `.claude/` / `.env.example`-template only; no `migrations/` or `shared/schema/` change (the merge pulled in main's calculator code, already logged in the row below — this batch introduced no code). Migration HEAD on `main` remains `0011`. | KB index guard **PASS** (80 docs, all indexed, no dead links); corpus relative-link check clean — 0 dead links outside `archive/` (17 pre-existing malformed code-links inside the archived logs left as-is under their "never act on" banner). All 3 prod deploys **READY** on `mortgage-stream.vercel.app`: `c504f1a` = `dpl_2Y5pgRswxvbAPehEDAu4ifmgmznv` (vite, `iad1`, ~72s), `c30758a` = `dpl_CWtt7xsC5QPnmzLDsvJEYnsRKhyw`, merge `6c6fc77` = `dpl_CaVxVRqjqW2YnHHZjcXQxsx7WiuU`. This ledger row logged in a follow-up commit. | [ROLLBACK.md](./ROLLBACK.md) §1; docs/config only, safe — `git revert <sha>`. Reverting `bba03b7` restores `kb/logs/` from `kb/archive/`; `ac1f12f` removes the skills; the rest are self-contained. |
| 2026-07-08 | **Public calculator suite + pre-license surfacing** (3 deploys, newest→oldest): **PR #88** — educational "Open the calculators" link on `client/src/pages/public/Waitlist.tsx` (merge `d284fc9`); **PR #87** — moved the "Calculators" nav link **outside** the `!PRELAUNCH_GATED` block in `Navigation.tsx` so it shows in gated mode while Rates/Buy/Refinance stay gated (merge `ce9ed6b`); **PR #86** — 5 new calculators (`/calculators/amortization`, `/home-equity`, `/payoff`, `/down-payment`, `/bah`) + the `/calculators` hub, all `PublicPage`, deterministic client-side math on the `MortgageCalculator` pattern (merge `a5dd472`). ⚠️ **AUTHORIZED DEVIATION from [ARMED_LAUNCH_CHARTER](../governance/ARMED_LAUNCH_CHARTER_2026-07-07.md) BUILD-1**: these calculators render **priced results + "Get Pre-Approved" CTAs on unauthenticated routes while the gate is on**, which BUILD-1's acceptance forbids ("no unauthenticated route renders a rate, a priced quote, or an apply/get-approved CTA"). Flagged to product with the compliant alternative (gate them like `/calculators/rent-to-own`); **product elected to override (2026-07-08)** to surface the free tools pre-launch. Mitigations: educational link framing, no Reg Z trigger terms / approval language in the link copy, in-code deviation note on `Waitlist.tsx`. **OPEN COMPLIANCE ITEM — requires counsel sign-off before F1**; a Lane-1 gate-integrity audit will flag these (plus the 3 pre-existing ungated calcs — affordability/mortgage/rent-vs-buy) as leaks. | **None** — no `migrations/` or `shared/schema/` change; `calculator_type` is an existing free `varchar(50)`, so the new types (`amortization`/`home_equity`/`mortgage_payoff`/`down_payment`/`bah`) persist without a schema change. Migration HEAD on `main` remains `0011`. | `tsc` clean + `npm run build` clean (#86); per-calculator live math spot-checks on a booted worktree server (e.g. $320k @ 6.5%/30yr → **$2,023/mo**, $408k interest; +$300/mo → 8yr 10mo sooner; BAH $2,400 → **$314,841** VA price, PITI == budget); nav + waitlist ungating verified with `VITE_PRELAUNCH_GATED=true` (Calculators shows, Rates/Buy/Refinance hidden; waitlist link → hub); **zero console errors** across all surfaces. All 3 deploys merged to `main`. | [ROLLBACK.md](./ROLLBACK.md) §1; each PR is an isolated merge commit — `git revert -m 1 <merge-sha>`. **Reverting `d284fc9` (#88) + `ce9ed6b` (#87) restores BUILD-1 compliance while keeping the calculators** behind the gate; add `a5dd472` (#86) to remove the suite entirely. |
| 2026-07-08 | Docs-reconciliation push (**PR #83**, docs-only): two-pass `.md` corpus audit — removed stale/conflicting info and completed indexing across `README`/`CTO_ROADMAP` + `knowledge-base/`; closed `FINDINGS` `D-001`–`D-007` + `F-001`. This ledger row rides in the same PR. | **None** — docs-only; no `migrations/` or `shared/schema/` change (22 `.md` files). | KB index guard **PASS** (82 docs, all indexed, no dead links); zero dead code-file refs; zero broken `.md` links corpus-wide. | [ROLLBACK.md](./ROLLBACK.md) §1; `git revert -m 1 <merge-sha>` — safe, docs-only |
| 2026-07-08 | Ledger + borrower-tracker batch (7 deploys, one folded row): **#77–#80** docs — production change ledger maintenance (#77 07-08 batch + retroactive 07-06 rows, #78 prod migration-HEAD `0011` confirm, #80 #70 row) and **#79** the F1 `PROD_ACCEPTANCE_TEST` launch-gate checklist; **#81/#82/#84** borrower `JourneyTracker` — de-jargoned milestone labels (#81), `closing`/**Signing** split into its own milestone (#82), responsive compact-on-mobile layout (#84). (#83 docs-reconciliation carries its own row above.) | **None** — no `migrations/` or `shared/schema/` change; latest migration on `main` remains `0011`. | Code PRs #81/#82/#84: `tsc` clean + full unit suite **739 green** (nothing depends on the tracker's internal DOM). Docs PRs #77–#80: KB index guard clean. All deploys **READY** on `mortgage-stream.vercel.app`. | [ROLLBACK.md](./ROLLBACK.md) §1; each is an isolated merge commit — `git revert -m 1 <sha>` (e.g. `ffac818` for #84) |
| 2026-07-08 | **PR #70** — borrower consent revocation: `POST /api/consents/:consentType/revoke` (allowlisted to `tax_document_use`; credit consent keeps its staff-gated workflow) flips `borrower_consents.isRevoked` and purges the user's derived `tax_insights` rows so the staff DSCR feed + borrower graph stop reading them; writes a `consent.revoked` audit entry. Rebased 56 commits onto `main` (resolved the `storage.ts` CPA-methods conflict; caught + fixed a stray rebase conflict marker before merge). Merge `b283910`. | **None** — no migration: the `is_revoked` / `revoked_at` / `revocation_reason` columns already exist on `main`; the purge is a runtime row delete scoped to the session user (encrypted extraction lineage stays on the source document). Migration HEAD remains `0011`. | `tsc` clean; full unit suite **739**; integration `taxInsightRoutes` **10/10** incl. 4 new revocation cases (401 unauth · 400 non-revocable · revoke+purge+re-lock · 404 no-active), run against a locally-booted worktree server; **§9 security review PASS** (session-scoped revoke+purge, no IDOR, allowlisted, audited); prod deploy `dpl_DVekjPRHC7qwfZJJ85T3p95p4bs4` **READY** on `mortgage-stream.vercel.app` (built ~70s, `iad1`) | [ROLLBACK.md](./ROLLBACK.md) §1; `git revert -m 1 b283910` |
| 2026-07-08 | Cleanup-to-MVP batch — 10 PRs merged to `main` (newest→oldest): **#76** uploads dev-only local-storage fallback (§9 reviewed); **#64** MISMO F-018 `validateMISMOXML` follow-through (drop `BaseLoanAmount` from required set); **#75** MISMO F-019 valid `LoanPurposeType` enum + fail-loud construction; **#63** Buying Power Estimator + SEO articles; **#61** Approval Strength tool; **#74** KB reconcile #68/#69; **#73** KB consolidation + index guard; **#71** design-comment truth pass; **#69** L1 VISION_AND_SCOPE (its own deploy was superseded/canceled by the next push; content landed via #74); **#68** QA infra Phase 0. Current prod = merge `920b34a` (#76). | **None** — verified no commit in this batch touched `migrations/` or `shared/schema/` (`git log --since=2026-07-06 -- migrations/ shared/schema/`): code + docs only. Latest migration on `main` remains `0011`. | Full unit suite **739 green** + `tsc` clean pre-merge across the code PRs; **§9 security review PASS** on #76 (uploads — recorded on the PR); prod deploy `dpl_9RBz4K5Bxp3R418GMu4fh4DgyQHv` **READY** on `mortgage-stream.vercel.app` (built ~70s, `iad1`) | [ROLLBACK.md](./ROLLBACK.md) §1; each PR is an isolated merge commit — `git revert -m 1 <merge-sha>` reverts one (e.g. `920b34a` for #76) |
| 2026-07-06 *(retroactive — logged 2026-07-08)* | Tax Return Insight pipeline: **PR #55** (P0 consumer-direct tax upload → readiness signals) + **PR #66** (P1 CPA inviter-only referral portal) merged to `main`. | Migrations `0009` (lender_submission_package — **the row previously flagged PENDING on the 2026-07-05 entry below**), `0010` (tax_insights), `0011` (cpa_partners). Prior-session record indicates all three applied to prod on 2026-07-06 via raw `pg` client — per-migration transaction + manual `drizzle.__drizzle_migrations` insert (Neon-pooler workaround, see [TEAM_PRACTICES](../governance/TEAM_PRACTICES.md) §5), after finding prod stuck at `0008`. ✅ **Confirmed 2026-07-08: prod migration HEAD = `0011`** — a read-only check found **12/12 rows** in `drizzle."__drizzle_migrations"` (0000–0011), matching the repo journal. No pending migrations on prod. | Unit + integration green pre-merge; a HIGH review escalation (a self-registerable role added to `STAFF_ROLES`) was caught and fixed in `ae06fd4` before merge. | [ROLLBACK.md](./ROLLBACK.md) §1, §3 (migration rollback) |
| 2026-07-05 | Close-out push `049a7fc→809eb7c`: PR #52 (docs SDLC) + PR #51 (LS-10 slice 2 — lender MISMO package assembly). Includes this ledger row (docs follow-up). PR #53/#54 (overlapping pre-launch gates) held for coordination. | ⛔ **Migration `0009` (lender_submissions package columns) PENDING on prod** — founder-supervised `npm run db:migrate`; idempotent `ADD COLUMN IF NOT EXISTS`, and the write path is F1-gated so not urgent, but must precede any real lender submission | 656 unit + 73 integration green pre-push; server boot + `/api/health` 200 on :5002 | [ROLLBACK.md](./ROLLBACK.md) §1; `git revert -m 1 809eb7c` isolates the two PRs |
| 2026-07-04 (evening) | Launch-integration batch: 13 PRs (#37, #39–#50) merged to `main` in one founder-authorized push | none (migrations 0005–0008 already applied) | 647 unit + 73 integration tests green pre-push | [ROLLBACK.md](./ROLLBACK.md) §1–2 |
| 2026-07-04 | PR #38 — GSE delivery engines (SFC/QM/edit mirror), broker submission workflow, lender submissions, dual-AUS | Migrations 0005–0008 applied via direct `pg` client + manual journal insert (Neon pooler breaks `npm run db:migrate` — see the known-traps index in [TEAM_PRACTICES](../governance/TEAM_PRACTICES.md) §5) | Deploy verified live post-migration | [ROLLBACK.md](./ROLLBACK.md) §1, §3 |
| ≤ 2026-07-04 | Baseline — everything before this ledger existed: migrations `0000`–`0004` and all prior deploys | Verified applied/live in the 2026-07-04 source-of-truth audit | [ASSUMPTIONS.md](../governance/ASSUMPTIONS.md) §4 | — |
