# Routine claim register

**The lock, and the only one.** Every routine and every human session that intends to **write
code** claims its target here first. Rules live in [`CHARTER.md`](CHARTER.md) §5; this file is the
table plus the overlap protocol.

Without this, the **four daily build lanes** — Primary Engineer (07:15, company-wide), Capture Path
Engineer (09:10, capture path), Workflow Completion Engine (09:53, one seam), Feature Completion
Engine (12:34, one domain) — plus Refactor Radar (Sun 20:00) all write to overlapping code with no
idea the others exist.

> 🚨 **The 2026-08-19 suite rewrite doubled the number of routines that need this board** (two daily
> code-writing seats → four) and their windows are only ~40–150 minutes apart, so a long run
> overlaps the next by construction. Claim before you write; treat an open PR as outranking the
> board; release in the same PR as the work. See [`CHARTER.md`](CHARTER.md) §3 for the new clock.

> **Absorbed `knowledge-base/SESSION_CLAIMS.md` on 2026-08-12.** Two boards briefly existed — this
> one from PR #493 and SESSION_CLAIMS from PR #496, written hours apart by sessions that could not
> see each other, which is the very failure both were built to prevent. This file is authoritative;
> that one is now a pointer stub. Its graduated overlap protocol, shared-file hazards and live claim
> are below; its finding-id scheme and signal hierarchy moved to `CHARTER.md` §5 and §10.

## A claim is a courtesy, not a mutex

Nothing enforces it. It costs one commit and it degrades toward *available*, never toward *blocked
forever* — a session that dies mid-run cannot clean up after itself, so a board that latches would
be worse than no board.

**The stronger signal is always `origin/main`.** A file with an open PR against it is claimed by
that PR, board row or not. Read commits and open PRs first; read this board second, for the intent
that `main` cannot show until work lands.

## How to use it

1. **Claim before writing.** Add a row: routine, target, worktree, branch, UTC timestamp, intent.
2. **Respect fresh claims.** Target claimed **< 24 h** ago → do not race it; take the graduated
   response below.
3. **Reclaim stale ones.** A claim **≥ 24 h** old with no open PR or commit behind it is dead —
   take the work and say so in your report. A claim of any age that *does* have an open PR behind
   it is live, because `main` outranks this page.
4. **Release on finish.** Delete your row whether you shipped, abandoned, or failed, in the same PR
   as the work. A board nobody clears becomes a board nobody reads.
5. **Commit the claim** on your branch so peers can see it. Never push to `main`.

Humans claim too — a routine cannot see your editor.

## Before you touch someone else's PR — the check `gh` cannot do

Added 2026-08-22, after a drain session update-branched PRs that had **live interactive sessions
attached to them**. Neither `gh pr view` nor `ListAgents` shows this. A peer had to say it out loud.

```bash
# each row carries prNumber + isRunning — test it, do not eyeball the sidebar
mcp__ccd_session_mgmt__list_sessions
```

`isRunning: true` with a `prNumber` means **a human or session is editing that PR right now**.
Refreshing its branch or merging it pulls the rug out mid-edit. On 2026-08-22 that was **two**
sessions on #650 and one each on #653 and #660 — invisible from the CLI.

**Rule: run this before any `gh pr update-branch` or merge on a PR you did not open.**

### Three things that look like a CI verdict and are not

All three cost real time on 2026-08-22:

1. **`main` itself can be red, and it fails everyone.** `guard:citations` failed on pristine
   `origin/main` (24 → 29). CI tests the **merge-with-main** commit, so **13 open PRs went red on a
   step none of them owned**. Two sessions diagnosed it independently from opposite directions
   before either attributed it correctly. **Before debugging your own diff, run the failing guard
   against pristine `origin/main`.** Fixed in #662.
2. **A draft PR accumulates `statusCheckRollup` rows that read as coverage.** The gate's `if:`
   includes `draft == false`, so it never ran — but #653 showed `checks=6` while ungated. The
   failure mode is not an obvious blank, it is six green-ish rows. Same class of lie as
   `mergeStateStatus: CLEAN`, which only ever meant "no conflicts". `gh pr ready` to get a verdict.
3. **A rerun of a billing-era corpse can be a second corpse.** `gh run rerun` re-queues under the
   *current* billing state. Check `steps` on the **rerun**, not just on the original: a real gate
   run is 28–29 steps / ~3 min; a corpse is `steps=0` in ~2s.

## Graduated overlap response

When your intended work meets a live claim, the answer is rarely "stop":

- **No overlap** → proceed; add your claim.
- **Adjacent** (same area, different files) → proceed, add your claim *naming the adjacency*, and
  keep your diff inside the files you listed.
- **Direct overlap** (same files, or the same finding-id space) → do **not** race. Prefer different
  work, or work the assist ladder in [`CHARTER.md`](CHARTER.md) §5 against what is already in
  flight. If the work genuinely must happen now, coordinate — `SendMessage` if the session is
  reachable, otherwise a note in the row's Notes for the owner to sequence.

## Active claims

| routine / session | target | worktree | branch | claimed (UTC) | intent |
|---|---|---|---|---|---|
| founder-directed session (Selling Guide constitution) | `scripts/selling-guide-authority-guard.cjs` (new) · `tests/sellingGuideAuthorityGuard.test.ts` (new) · `.github/workflows/ci.yml` · `package.json` (one script line) · every `.claude/skills/<slug>/SKILL.md` · `.claude/agents/_OWNER_RAILS.md` · `knowledge-base/governance/TEAM_PRACTICES.md` | scratchpad/sg-wt | compliance/selling-guide-constitution | 2026-08-21T12:20Z | Installs the Selling Guide as the governing authority: the §10 authority gate, the Guide-first rewrite of the skill fleet, and the shared owner rails. **Deliberately NOT claimed** — `CLAUDE.md` (#650, #642), `knowledge-base/routines/CHARTER.md` (#647), `knowledge-base/routines/TEAM.md` (#645) and SELLING\_GUIDE\_CONFORMANCE.md (#650, not yet on main) are all in flight; those edits wait for those PRs rather than racing them (§5: a file in an open PR is claimed). The gate ships **inert** until the corpus from #650 lands, and arms itself automatically then. |
| founder-directed session 2026-08-23 (Selling Guide conformance scrub) | `knowledge-base/compliance/SELLING_GUIDE_CONFORMANCE.md` · `knowledge-base/compliance/selling-guide-coverage.json` (+ the generated map) · `data/regulatory/regulatory-ledger.json` · citation comments only in `server/services/{preUnderwriting,scenarioCatalog,underwritingNuance,loanAnalysis,mismoValidation,ausSubmission}.ts`, `server/services/autopilot/followUps.ts`, `server/underwriting.ts`, `server/scripts/seedLendingGrids.ts` · borrower copy in `client/src/pages/education/glossaryData.ts` + `client/src/pages/realtor-engine/ScenarioDesk.tsx` · a new selling-guide-conformance-guard.cjs (not yet written, so deliberately not cited as a path here) + `scripts/selling-guide-authority-guard.cjs` · `tests/{autopilotFollowUps,underwritingNuance,sellingGuideAuthorityGuard,ciTriggers}.test.ts` + its new sellingGuideConformanceGuard test | (primary checkout) | claude/selling-guide-conflicts-7ls3ct | 2026-08-23T00:00Z | Remove what conflicts with the Guide and install the loop that keeps finding it: heal the register's own C-9/C-10 id collision, fix two mis-citation clusters (seasoning → B3-3.5-01, large deposits → B3-4.2-02), single-source the duplicated B3-6-05 constants, correct borrower-facing agency claims, disposition the G-6 revised-section queue, and add `guard:conformance` + the documented scrub loop. **No policy value changes and no behavior changes** — every loosening-direction finding is held as a PR escalation per the standing rule. **Deliberately NOT claimed:** `shared/fannieMae/**` and `server/pricing.ts` (job-aid authority / LLPA Matrix unprocured), the four delivery-stack freeze-guard files, and the extractor's pinned constants. |
| founder-directed drain session 2026-08-22 | **the merge queue itself** — open-PR triage, `gh pr update-branch` refreshes, merges to `main`, and branch cleanup. Plus `scripts/citation-baseline.json`. **NOT claiming any feature code**: a PR's own content stays its author's. | main-probe, reg-watch-0820 | `chore/claim-drain-lane-0822` | 2026-08-22T18:45Z | Drain the 20-PR backlog that built up during the Actions billing outage. **`main` was RED** (citation ratchet 24→29) which failed 13 PRs for a reason none of them owned — fixed in #662, so re-run before you believe an old red. **If you own an open PR: I will `update-branch` and merge it when green; I will not edit its content.** Overlap here is adjacency, not collision — but tell me before you merge to `main` so we do not double-deploy. |


## Recently released

| founder-directed close-out session 2026-08-20 | the merge queue itself — conflict resolutions inside PRs #624 #598 #619 #617 #607 #623 #605 #628 #632 #630 #636, baseline/table regenerations, branch deletions, the close-out report | (three drain worktrees) | released same day | **shipped** — 19 open PRs + ~30 bare branches drained to 4 open (1 parked on the billing outage, 2 review drafts); 21 PRs' content merged, locally gated end to end; 14 dead branches tombstoned; the #615 stacked-base incident caught and recovered as #630 (`baseRefName` is now a mandatory pre-merge check). Full narrative: [logs/2026-08-20-merge-queue-close-out.md](../logs/2026-08-20-merge-queue-close-out.md). Standing anti-drain rules now bind future sessions: drain before build · done = merged + health-verified · no bare branches (first commit → draft PR) · docs-only routine reports ride a fast lane. |

| hq-aus-autopilot-owner (implement mode) | the borrower Autopilot banner's status snapshot | aus-autopilot-0820 | claimed 2026-08-20 11:20, released 12:05 | **shipped** — `buildAutopilotStatus` said "Looks good! No issues found." on a file with zero conditions *and* when the conditions read threw. Two new phases (`no_items_yet`, `unavailable`) so the snapshot only asserts what it read. Adjacency noted: the one-line generated §0 count in `DESIGN_SYSTEM.md` is also touched by PR #619 — regenerate whichever lands second. |

| primary-engineer 2026-08-20 item 1 (F-0819-03) | `server/routes/borrower/{guaranteesHomeowner,homeownerProfilePayload}.ts` · `server/services/lifecycleEngine.ts` · `client/src/pages/homeowner/homeownerDashboard/{EquitySection,RefiAlertsSection,types}.ts(x)` · `tests/homeownerHubWrites.test.ts` | claimed 2026-08-20 10:40Z, released 11:05Z | **shipped to review** — the Hub's only entrance, the setup form, had never worked: nine controls initialised to `""`, posted whole, straight into Drizzle, whose `timestamp` mapping calls `.toISOString()` on the value. Every owner got a 500, so nothing behind the door was reachable. Same shape on the annual-review PUT and on both in-Hub buttons (NOT NULL columns the client never supplied). Both profile writes now validate through `homeownerProfileWriteSchema` — **a blank control is an unanswered question, so it drops and the column stays NULL**, never `new Date("")`. The two action endpoints stop inserting the client's body and call the daily sweep's own legs (`resolveHomeownerPosition` / `recordEquitySnapshot` / `evaluateRefiOpportunity`, extracted from `sweepProfile` with its sequencing preserved exactly) — which keeps the **EPO clawback suppression and the open-alert de-dup on the borrower's path**, where a client-composed alert bypassed both. **Zero calculation changed.** Both buttons report the real outcome instead of an unconditional success toast. Mutation-proven both ways. **Peers:** `lifecycleEngine.ts` now exposes those three legs — reuse them rather than hand-rolling an equity or refi derivation, and treat `""` from a controlled input as *absent*, not as a value. |

| primary-engineer 2026-08-20 item 2 (F-0819-10) | `client/src/components/NotificationsPanel.tsx` and `client/src/components/NotificationsPanel.test.tsx` | claimed 2026-08-20 11:20Z, released 12:05Z | **shipped to review** — read state was keyed off `readAt`, a column the `notifications` table has never had (`markNotificationRead` sets `status: "read"`; the bell counts `status = 'unread'`). `!undefined` is `true`, so **every notification rendered unread forever**: clicking one marked it read, the panel refetched, and it came back looking identical while the bell count beside it went down. Program-scale, ECOA adverse-action notices included. Now `isNotificationUnread(n)` reads `status`, treating anything not explicitly `"read"` as unread — the fail-safe direction. **Why it survived: the colocated test hand-wrote `readAt: null` into its fixture**, so the fixture supplied what the product could not and the dead branch stayed green; three new assertions now read the *server* files (the mark-read write, the unread count, the absent `read_at` column) so the two sides cannot drift apart silently. Mutation-proven. **Peers: a fixture field that no endpoint sends is the tell** — check the payload against `shared/schema/` before trusting a green colocated test. |

| directed session (F-077 FHA leg) | `server/services/{loanEstimate,loanCosts,scenarioSimulator,mortgageInsurance,apr}.ts`, `tests/loanEstimateMI.test.ts` | claimed 2026-08-18T15:18Z, **cleared by evening-triage 2026-08-20T02:30Z** | **stale claim cleared under rule 3** — 35 h old, and the stronger signal says it is finished: **PR #556 merged 2026-08-18T20:04:58Z**, branch `claude/musing-engelbart-0a72db` deleted from origin. The owning session never removed its row (rule 4). Cleared rather than carried, because a claim that outlives its PR blocks every peer off `server/services/**` for nothing. |

| founder session (interactive) | borrower dashboard (UC-007) + the `arbitraryColorValues` metric split | `claude/routines-operating-cadence-olj41t` | claimed 2026-08-18 19:35, released 19:55 | **shipped** — §15 was already built; fixed `min-h-screen`→`min-h-full`, a skeleton at the retired `max-w-xl` that snapped ~3x wider on load, 7 bespoke type rungs, 1 sub-44px control. Raised **UC-008**: the icon registry covers 36 of the 176 glyphs the app uses, which is why it has zero adopters — extending it is founder-gated. |

| founder session (interactive) | URLA capture path — `URLAForm.tsx` JSX + five `urla/` sections | `claude/routines-operating-cadence-olj41t` | claimed 2026-08-18 18:45, released 19:05 | **shipped** — rail no longer scrolls horizontally; 14 sub-44px controls raised via `.touch-target`. Presentational only: the trap doc's five hazards were audited against the diff (no `buildPayload`/`STEPS`/hydration move, no memo/useCallback/debounce, latch untouched). The two structural §12 gaps (tunnel vision, ≤3 inputs) are **proposed, not shipped** — both need decisions above a layout call. |

| founder session (interactive) | pre-approval funnel capture path | `claude/routines-operating-cadence-olj41t` | claimed 2026-08-18 18:05, released 18:15 | shipped — 320px grids, step-input rung, AdvisoryPanel reaches mobile, full-width CTA, canonical gutter. `FunnelChrome.tsx` claimed but NOT edited: the §12.2 disclosure-placement question is compliance-bearing and went to the founder instead. |

Keep the last ~10 for collision forensics; trim older rows freely.

| routine / session | target | released (UTC) | outcome |
|---|---|---|---|
| directed session 2026-08-23 (handoff harness tiers) | `scripts/handoff-facts.cjs` (`--cite`) · `scripts/doc-freshness-guard.cjs` | 2026-08-23T03:43Z | **shipped to review, narrowed** — branch `chore/harness-tiers-0823`. Reduced on 2026-08-23 08:10Z to the two holes `main` still has: `--cite` had skipped 277 of 832 citations and bound no symbols (now resolves bare basenames and checks a named symbol is on its line — 28 ambiguous · 9 backwards ranges · 4 slid on `6377727e`, red by design; the docs PR fixes the citations), and the handoff corpus's 28 `Freshness:` lines were enforced by nothing (`handoff/README.md` + `FACTS.md` join `REQUIRED`). The `harness:t0..t3` aliases, the ralph-state `.gitignore` and the pre-push/preflight parity this claim also covered were landed first by #702 and #703 (a parallel session) — dropped here, not duplicated. No server/client code. |
| feature-completion 2026-08-20 | `shared/acceleratorProgram.ts` (new) · `server/routes/borrower/realtorPrograms.ts` (accelerator block only) · `client/src/pages/education/acceleratorProgram/{ProgramHeader.tsx,types.ts}` (+ new `client/src/pages/education/acceleratorProgram/ProgramHeader.test.tsx`) · `tests/acceleratorProgress.test.ts` · `vitest.config.ts` | 2026-08-20 | **shipped to review** — [PR #632](https://github.com/barakatammre84/Homiquity/pull/632), area 22 (Homebuyer accelerator, `Last reviewed: never`). The program's phase never advanced: nothing had ever written `current_phase` (`git log -S` → initial commit + the #193 split only), so 18 ticked milestones still rendered *Phase 1 of 6 · 17%* — while `MilestonesSection` had been invalidating `/api/accelerator/enrollment` on every toggle all along, expecting the write. Progress is now **derived from the milestone set, never incremented**, so un-ticking walks the program back; proven live against Postgres on :5002 (phase 1→2→3→4→5→6→`completed`, then back to 2 on one un-tick). Also fixed the second name for the same six phases: the client's `PHASE_NAMES` had drifted from the categories the server seeds, so the header called phase 1 "Foundation" while every milestone in it was badged "Financial Assessment". **⚠️ §5 sequence deviation, stated rather than back-dated:** the stronger signals §5 itself ranks first were checked before writing (`git fetch`; all 28 open PRs' changed files), but this row was written at commit time, not before the edit. **Adjacency, not overlap:** PR #605 holds `EnrollmentView.tsx`, `FinancialUpdateDialog.tsx` and `ScheduleSessionDialog.tsx` in this same directory — none of the three is touched here. **Peers:** `shared/acceleratorProgram.ts` is now the one definition of the six phases; seed from it and render from it rather than writing a second phase-name map. **Three gaps refused, not missed** — the coaching session that books nothing (founder call: do we staff coaching?), the missing door to `/accelerator` (deliberately left shut until that is answered), and the unvalidated `req.body` on both enrollment writes. All three are proposed tickets in [the run report](knowledge-base/routines/reports/2026-08-20-feature-completion.md).

| workflow-completion 2026-08-20 | `server/services/extractionPersistence.ts` (new) · `server/routes/lending/documents.ts` · `server/routes/documents.ts` · `tests/extractionPersistence.test.ts` (new) · `vitest.config.ts` · `FEATURE_MAP.md` + `hq-documents-owner` scope | 2026-08-20 | **shipped to review** — [PR #628](https://github.com/barakatammre84/Homiquity/pull/628). Workflow 4 step 3: extraction reaches a document by **two** routes and only one is a borrower's. F-028 (`persistDocumentFacts`) and F-030 (`wireExtractionToReadiness`) had exactly one call site each — the staff `POST /api/documents/:id/extract`, whose only client caller is `DocumentReviewPanel.tsx`. The borrower's own upload auto-extracted their pay stub and threw the values away, plus all model/prompt lineage. Post-extraction behaviour now lives once in `server/services/extractionPersistence.ts`; **add to the helper, never to a call site**. Mutation-proven (reintroduce → exactly 3 red, the MR-2 no-self-verify and low-confidence gates stay green). `detectTriggers()` `[]`. Peers: `server/routes/lending/documents.ts` was in **no** FEATURE_MAP area — added to area 11. |

| frontend-wiring-audit 2026-08-20 | `client/src/pages/lending/loanPipeline/LoanSummarySection.tsx` (+ new colocated test) · `DESIGN_SYSTEM.md` §0 generated table (one number) | 2026-08-20 | **shipped to review** — [PR #619](https://github.com/barakatammre84/Homiquity/pull/619), branch `claude/infallible-gould-f06a1b`. `NeedHelpCard`'s two controls were leaf `<Button>`s with no handler and no wrapping trigger: the bottom card of the borrower's own file page said "your loan officer is here to assist" and neither button did anything. **"Schedule Call" is removed, not wired** — the product has no scheduling at all (0 hits for `calendly|schedule a call|book a call|consultation` across `client/ server/ shared/`), so it was a promise nothing could keep (DESIGN_SYSTEM §13 honesty). "Contact" now goes to `/messages` via `<Button asChild><Link>` (§12.4); the destination has a recipient even with no deal team assigned — `GET /api/team-members` falls back to all staff (`server/routes/borrower/messaging.ts:28-37`). Proven by reintroduction (3 of 3 red pre-fix). §9 `detectTriggers()` → `[]`. **Peers: the inert-control census behind this found 5 more dead controls, all in open PR #605's files** — `ApplicationSummary.tsx:85,126,209,256` (the registered ux-26, including an `EmptyState`'s only action) and `GapCalculator.tsx:264` "Apply Now" inside a *Goals Complete* card. Not raced, per rule 2. ⚠️ #605 is the touch-target sweep: it makes those buttons **bigger, not working** — whoever lands it should fix them or say plainly that it does not. Also: `git stash` is REPO-WIDE across worktrees and `pop`/`drop` are permission-blocked unattended — `stash@{0}` is this run's own leftover, not abandoned peer work. |

| frontend-wiring-audit 2026-08-19 | `client/src/pages/borrower/URLAForm.tsx` (application-progress scope) + `URLAForm.test.tsx` | 2026-08-19 | **shipped to review** — branch `claude/agitated-chatterjee-af3449`. The URLA progress bar is labelled **"Application progress"** but was computed from the ACTIVE borrower slice, so a file with a co-borrower got two answers to one fact: "5 of 7 sections complete" on the primary tab with the co-borrower's six sections empty, falling to "1 of 7" on switching tabs (DESIGN_SYSTEM §13 Agreement; same class as the 08-18 `TaskProgress` denominator). It now counts every per-borrower section once per borrower and the shared property/loan section once, so the number cannot change with the tab and the numerator only rises. Rail check marks deliberately left per-borrower — the "Editing for:" control names whose. **Mutation-proven** (restore `STEPS.filter(...)` → exactly the 2 co-borrower assertions red, reading "5 of 7" and "1 of 7"). URLA_FORM_REFACTOR_TRAP respected. **Peers — two findings this run are NOT mine and are unclaimed:** (1) `server/routes/lending/statusDecisions.ts:78-84` `UPDATABLE_COLUMNS` omits `householdFamilySize`, `homeSquareFootage` and `avoidsInterestFinancing`, so the funnel's draft autosave validates then silently discards them (§6b = Backend Data Engineer; the two VA fields need `parseInt`, they arrive as strings); (2) **PR #587** — was red on a stray copy of #596's ux-30 files; **its own session fixed that at 13:38Z** and it is green. It is now a *merge decision*, not a defect: **#594 (merged `24256ff1`) already fixed the same five overflows from the container end** (`grid-cols-1` → `repeat(1,minmax(0,1fr))`), so #587's CTA-side fix is defence-in-depth and its body's "five routes overflow" is no longer true of `main`. **#542's content is audited** in the same report — ~90% already landed byte-identically; the real remainder is the missing NMLS number in the funnel footer, three VA constants re-stated as literals in `underwritingEngine.ts:440,490,491` that `underwritingNuance.ts:206-208` already exports, and a `Footer.tsx` prelaunch gate that is a no-op in today's prod. Both written up with patches in `reports/2026-08-19-wiring-audit.md`. |
| qa-sweep 2026-08-18 (founder-directed fix) | `client/src/components/SubmissionReadinessDialog.tsx` (F-0818-09, the lender-identifier mismatch) | 2026-08-18 | **shipped to review** — the founder directed the fix in-session, which is outside QA Sweep's §6 territory ("findings only"); deviation recorded in the run report and the PR. ⚠️ **§5 sequence deviation, stated rather than back-dated:** I verified the target was unclaimed via the stronger signals §5 itself ranks first — `git fetch`, then every open PR's changed files (0 of 6 touch this file) — but wrote the code before adding this row. Colocated test added (the companion coverage-gap F-0818-10 is why the bug survived 13 days); fix proven by reintroducing each of the three legs and confirming exactly one test reds each time. |
| founder-directed session (pattern library + seven surface migrations) | `client/src/components/patterns/**`, the seven migrated surfaces, `lib/dualUnitMath.ts`, `index.css`, `App.tsx` | 2026-08-18 | **MERGED** — [PR #563](https://github.com/barakatammre84/Homiquity/pull/563), squashed as `83f00e4`; `verify-deploy` green, so prod serves it. **Row cleanup note:** this umbrella claim should have been deleted in that PR under rule 4 (its sub-lane rows were released, this one was missed) and its branch is deleted, so it was a stale claim on `main` pointing at nothing. Removed here by the same session. Peers: the pattern library is shared ground — extend `components/patterns/**` rather than hand-rolling, and derive borrower counts from `lib/outstandingWork.ts`. |
| founder-directed session (one outstanding-work selector) | `client/src/lib/outstandingWork.ts` (new) + test · `components/BorrowerRequests.tsx` (+new test) · `pages/borrower/Tasks.tsx` (predicate only) | 2026-08-18 | **shipped to review** — the dashboard said "You're all caught up" while `/tasks` said "Needs Your Attention". `BorrowerRequests` selected `status === "OPEN"` alone, missing two states `/tasks` counts: **BLOCKED**, and **rejected** (`verificationStatus === "rejected"` on a non-closed task — a document to re-upload). `verificationStatus` was already in the borrower whitelist (`shared/borrowerTaskView.ts:59,188`); the component received it and never read it. Teardown §6.1 / §8, DESIGN_SYSTEM §13 Agreement. **The teardown's one-selector rule is now implemented**: `lib/outstandingWork.ts` is a pure structural module (`needsBorrowerAction`/`isRejectedTask`/`isPendingTask`/`selectOutstanding`/`countOutstanding`) that both task shapes satisfy, consumed by both surfaces — no endpoint or server change (§14). Empty state scoped: "Nothing to do on this loan", never an account-wide claim. **Mutation-proven** (restore the OPEN-only predicate → the rejected and blocked cases red). Peers: **derive every borrower count/badge/empty-state from `outstandingWork.ts`** rather than re-filtering task status inline — that re-derivation is what let two surfaces disagree. |
| founder-directed session (register the Complex File Engine routine) | `.claude/skills/complex-file-engine/SKILL.md` (new) · `knowledge-base/complex-file-engine/LEDGER.md` (new) · `routines/CHARTER.md` (§3 clock, §4 chain, §6 territory, new §6d, §3a finding) · `knowledge-base/README.md` | 2026-08-18 | **shipped to review** — branch `routine/complex-file-engine`. The suite's first routine on the **qualification** layer: every other control asks whether a file that qualified can be delivered; none asked whether the borrower could qualify at all, and a file that never produces a figure never reaches delivery. **Zero code files touched** — no `server/**`, `shared/**`, `client/**` or `migrations/**` in the diff, so no §5 claim was required. Registered **both** ways §11 demands, in one session: the in-repo definition and the local scheduler task `complex-file-engine` (`40 10 * * *`, observed fire 10:43, first run 2026-08-19). The trigger prompt carries the proven absent-skill STOP clause, so every tick before this merges is a one-paragraph no-op rather than an improvised underwriting edit. **Local fleet on purpose:** a live `RemoteTrigger list` read found **every CCR trigger `enabled: false` with zero run sessions ever**, while all ten local tasks had `lastRunAt` that day — recorded as a ⛔ founder item in §3a, because §6a and §6b assign accountable ownership of two lanes to routines in that table. **Peers:** the qualification layer (`server/services/income/**`, `selfEmploymentIncome.ts`, the tax/document intelligence modules, `shared/{incomePaths,situationProfile}.ts`) now has an accountable owner — the REGISTER is still the lock, but file a `CF-…` ticket rather than drive-by editing it. Seeded three verified findings and three verified-**not**-a-defect rows (declining-SE-income treatment, the P4 fs citation guard and the taxInsight import boundary all already exist — do not rebuild them). |
| founder-directed session (register the Backend Data Engineer routine) | `.claude/skills/backend-data-engineer/SKILL.md` (new) · `knowledge-base/backend-data-engineer/LEDGER.md` (new) · `knowledge-base/handbook/app-guide/12-api-contract.md` (new) · `routines/CHARTER.md` · `knowledge-base/README.md` | 2026-08-18 | **shipped to review** — branch `claude/backend-data-engineer-routine-m9lose`. The suite's first backend-lane routine, registered **both** ways CHARTER §11 demands in one session: the in-repo definition *and* the CCR trigger `trig_01K5iHLyJ8r9x6DG8KppW62o` (`0 11 * * *` UTC, first fire 2026-08-19T11:04Z), so nothing is registered-looking that isn't registered. §6b resolves the lane the §6a way — **accountable owner, shared lane**: the routine owns question A on the data side, Primary Engineer's company-wide lane is **not** narrowed, and the register stays the actual lock. The trigger carries the proven absent-skill STOP clause, so every tick before this PR merges is a one-paragraph no-op rather than an improvised backend edit. **Zero code files touched** — no `server/**`, `shared/**`, `client/**` or `migrations/**` in the diff; `pnpm check` 0, node 2851, client 641, `guard:kb`/`guard:docs`/`guard:staleness`/`guard:schema`/`guard:migrations`/`guard:querykeys` all green. A live `list_triggers` read also caught the fleet count drifting (the page said nine; it held eight enabled plus a retired one) — the stale-count hazard that section warns about, found in the section itself. **On merge with `main` that table had moved to §3a and already carried this routine's row**, added by another session from the same live trigger; §3a's version was kept and the inline one dropped. **Peers:** `handbook/app-guide/12-api-contract.md` is now the written UI↔backend boundary — the UI lanes may not change Zod schemas or payload shapes; file a ticket and this routine lands it. |
| founder-directed session (E-Consent revocation agreement) | `client/src/pages/borrower/EConsent.tsx` + new test file | 2026-08-18 | **shipped to review** — rides [PR #563](https://github.com/barakatammre84/Homiquity/pull/563). **A revoked consent still rendered as given.** `isConsentGiven` tested `consentGiven` alone; `/api/consents/me` → `getBorrowerConsentsByUser` is the one getter in `storage/locksAndConsents.ts` with **no `isRevoked` filter** (its siblings all have it), and `revokeConsentsByTypeAndUser` sets `isRevoked` while deliberately leaving `consentGiven: true` as the record of what was once given. So after the PR #70 revocation flow the page listed the consent under **Completed**, dropped it from **Action Required**, and told the borrower there was nothing to do — while `TaxReturnInsightCard.tsx:97`, reading the *same endpoint*, correctly tested `consentGiven && !isRevoked`. Two surfaces, one fact, opposite answers (DESIGN_SYSTEM §13 Agreement). Fixed client-side to match the known-correct predicate; **server storage/routes deliberately untouched** — narrowing that getter would change an endpoint two components share and discard revocation history, and server-side gates already use the filtered `getConsentByTypeAndUser`. **Mutation-proven** (ignore revocation → exactly the revoked-consent test reds). Also migrated the per-template control to `<ConsentField>`: the label now names the document instead of "the above", and the consequence sits outside it. New test file (the page had none). §9 audit per CHARTER §10 over all 50 changed files: no trigger. |
| founder-directed session (credit-consent honesty) | `client/src/pages/borrower/CreditConsent.tsx` + its test · `components/patterns/ConsentField.tsx` | 2026-08-18 | **shipped to review** — rides [PR #563](https://github.com/barakatammre84/Homiquity/pull/563). **A saved draft pre-ticked the FCRA hard-pull acknowledgment.** `CreditConsent.tsx` rehydrated `acknowledged` from the server draft, and that flag is the *only* gate before `consentGiven:true` posts — so a borrower returning to a draft found the authorization box already checked and could authorize a hard inquiry in a session where they never affirmatively acknowledged, possibly against a disclosure version they never saw. Fix: identity fields still resume, the acknowledgment never does; the draft still PERSISTS it (that record is untouched), it is only never restored into the live control. **Tightens a consent gate — the one permitted direction** (CHARTER §6). **Mutation-proven**: reintroducing the rehydration turns exactly the 3 gate tests red. Migrated to `<ConsentField>` with the e-signed authorization sentence preserved **byte-for-byte** (now asserted by equality, not a loose match) and the ratified ux-20 testids intact via new optional `checkboxTestId`/`labelTestId` props. **Defect-class sweep clean:** EConsent, ConsentGateCard, TaxReturnInsightCard and the funnel all hold consent in local state never rehydrated — this was the only instance. §9 audit run per CHARTER §10 (`detectTriggers()` over all 48 changed files): no trigger. |
| founder-directed session (borrower task/progress honesty) | `client/src/pages/borrower/Tasks.tsx` (+new test file), `Dashboard.tsx` (readiness label only), `components/patterns/TaskProgress.tsx` | 2026-08-18 | **shipped to review** — rides [PR #563](https://github.com/barakatammre84/Homiquity/pull/563). The live defect: Tasks recomputed its denominator every render, so an LO assigning a task mid-session dropped the borrower's percentage — the same completed work rendering as *less* progress (teardown §6.2). Now `TaskProgress`, denominator snapshotted, **mutation-proven** (un-freeze → 3 red incl. the page test; restore → green). `useTaskProgress` hardened to freeze on the first *meaningful* total, so a consumer rendering during load can't pin `0 of 0`. Zero state → scoped `EmptyState` (this application's tasks, never a global "caught up"); Dashboard's bare readiness % now states it is an estimate of how far the file has moved, with an aria-label on the bar. **JourneyTracker deliberately kept** — DESIGN_SYSTEM §15 names Dashboard.tsx the reference surface; swapping in `LoanTracker` would regress the exemplar. Peers: `text-status-*` resolve to hardcoded hex in `tailwind.config.ts:118-128` (33 sites) — that's `guard:ui` rawHexLiterals' lane, untouched here. |
| founder-directed session (UI-overhaul prototype, PR #558 stage 2) | `client/src/components/patterns/**` (11 new components + colocated tests) · `pages/staff/DesignPrototype.tsx` · `lib/dualUnitMath.ts` · `App.tsx` (one lazy route) · `index.css` (`--muted-foreground` → green-grey) | 2026-08-18 | **PR #558 closed unmerged (founder decision — the standard lands via #560's `DESIGN_SYSTEM.md` instead; two standards is the two-sources-of-truth failure).** The pattern library, token change, scrub and prototype are the recorded surviving work; they re-land via [PR #563](https://github.com/barakatammre84/Homiquity/pull/563) per the #558 close-out and #560's own "worth a follow-up PR" note. The §9 type-encoded invariants stay test-pinned; `ProvenanceBadge` derives from `shared/dataProvenance.ts` (no second enum — DESIGN_SYSTEM §13). Peers: `components/patterns/**` is shared ground — extend it rather than hand-rolling. |
| founder-directed hygiene session 2026-08-18 (ux-38 batch 3 — **closes ux-38**) | the last 20 nested-control sites: `Navigation.tsx` (11), `MobileBottomNav`, `not-found`, `ConversionCTA`, `FunnelChrome`, `SidebarCards` | 2026-08-18 | **shipped to review** — branch `claude/ux38-batch3-navigation`. Three distinct fixes, not one codemod: raw `<button>`s collapsed into their `<Link>` (styling wrapper, not a control); `<a>` wrappers → `<Button asChild>` with display utilities *translated* (`sm:block` → `sm:inline-flex`) rather than carried onto an inline-flex Button; onClick sites → `<Button asChild onClick>`, safe because wouter's Link calls the user handler before navigating (`wouter/src/index.js:298`). `nestedInteractive` **20 → 0** on the #560 guard; bundle −149 bytes; 584/584 client; tsc clean. ux-38 moved to FINDINGS Closed in this PR. |
| founder-directed hygiene session 2026-08-18 (ux-38 batch 2) | the 24 className-judgment `<Link><Button>` sites across 10 `client/src` files (the visual-regression surface batch 1 deliberately left) | 2026-08-18 | **shipped to review** — branch `claude/ux38-batch2-classname-sites`. Each site's Link classes were differenced against the Button's and only the genuinely-extra tokens moved (9 pure duplicates dropped, 10 `flex-1` moved so the Button becomes the flex child, 1 three-class move, 1 `block` dropped because the Button's own `w-full` already spans the row and `block` would fight its `inline-flex`). Cross-validated by the #560 UI ratchet, which tightened `nestedInteractive` **122 → 20** on this branch — 78 (batch 1) + 24 (batch 2) = 102, leaving exactly batch 3's scope. tsc clean; client lane 584/584; bundle at baseline (batch 1 already paid the eager-chunk cost); tokens at baseline. |
| founder-directed hygiene session 2026-08-18 (ux-38 batch 1) | the 78 mechanical `<Link><Button>` sites across 41 `client/src` files (AST-selected: Link's sole child is a plain `<Button>`, no className/spread/onClick) | 2026-08-18 | **shipped to review** — branch `claude/ux38-batch1-nested-controls`. Wrapper order swapped to `<Button asChild><Link>` via a TypeScript-AST codemod (positions, not regex); children and attrs verbatim; ±304 symmetric lines. Batches 2–3 (24 className-judgment sites, 4 onClick-intent sites, Navigation's 12 raw-`<button>` sites + the `<a><Button>` phone) deliberately not here — ux-38's plan keeps the visual-regression surface separate. tsc clean; client lane 582/582; tokens at baseline; bundle +44 raw bytes raised with cause (the `asChild` props on eager nav controls are the fix itself). |
| founder-directed hygiene session 2026-08-18 | `scripts/doc-staleness-guard.cjs` (new), `package.json`/`checkup.sh`/`ci.yml` wiring, 43 stale instruction-class references normalized across 25 living docs | 2026-08-18 | **shipped to review** — branch `claude/doc-staleness-ratchet`, opened same session on founder direction. Born from the knowledge-file audit §2b: a freshness line can be green over dead pointers, so vocabulary is now gated — five metrics covering the retired path root, the pre-pnpm package commands, the retired sprint-queue name, the former repo name, and the dead prod host; ratchet baselines 8/6/7/9/2 absorb documented history and only go down. `--list` prints exact lines. |
| user-tasked session (F-077 follow-up) | `server/services/scenarioSimulator.ts` MI source, `server/services/loanCosts.ts` (delete `calculatePMI`), `tests/scenarioSimulator.test.ts`, `tests/loanEstimateMI.test.ts` | 2026-08-18 | **shipped** — [PR #554](https://github.com/barakatammre84/Homiquity/pull/554). What-if MI now consumes each offer's product-aware matrix figure (offers are scenario-priced by construction); veteran-zero preserved as decision parity; `calculatePMI` deleted per its own docstring with the card figure pinned as a historical constant. Migration proven by reintroduction (2 red / veteran green both worlds). Heads-up for peers: draft #556 (FHA LE/decision MIP) stacks on this and cuts over to main after it lands. |
| primary-engineer 2026-08-18 (item 1) | ux-30 TRID delivery-window honesty: `server/services/loanEstimate.ts`, `server/routes/underwriting/delivery.ts`, `client/src/pages/lending/LoanEstimate.tsx`, `tests/trid.test.ts` | 2026-08-18 | **shipped** — [PR #546](https://github.com/barakatammre84/Homiquity/pull/546). Claim released on landing per rule 4; the branch had gone DIRTY on this register's own shared-file hazard (active-table divergence) and was resolved additively and landed by the #554 session on founder authorization. The stale 08-12 financial-audit row it also carried was already cleared by evening-triage on main — cleared side kept. |
| refactor-radar 2026-08-17 (owner-directed) | `client/src/pages/borrower/URLAForm.tsx` (RR-005) | 2026-08-17 | **extraction refused, prerequisite shipped** — [#530](https://github.com/barakatammre84/Homiquity/pull/530). The RR-005 extraction is refuted by 3 adversarial reviews (`handbook/URLA_FORM_REFACTOR_TRAP.md`), re-verified against the current 750-line file. Shipped only the union-narrowing of `STEPS[].id` that the doc names as its prerequisite. **Peers: do not re-attempt the extraction** — it is `blocked-human` in the radar ledger. |
| sprint-blitz 2026-08-12 | `server/services/mismoValidation.ts`, `server/routes/underwriting/compliance.ts`, batch reads in `server/storage/{urla,applications,pipeline}.ts` | 2026-08-12 | **shipped** — [PR #514](https://github.com/barakatammre84/Homiquity/pull/514), CTO_ROADMAP §3.2. Loading split from scoring; the compliance dashboard went from 15N+1 storage reads to a flat 4. New pure modules `server/storage/{batchGroup,urlaBatch}.ts` are shared ground for the next batching job — take them rather than hand-rolling a group-by. |
| `/financial-audit` (weekly, standing claim of 2026-08-12) | money paths (`shared/compensation*`, `costLedger`, `rateLockConfirmation`, `wholesaleLenders`, `contingentLiabilityRegister`, rate-sheets/rateLocks routes, admin Financial pages) · finding ids `F-0812-*` | 2026-08-17 | **stale claim cleared by evening-triage** (REGISTER rule 3): 5 days old, its branch `claude/fervent-mayer-oqk0iv` was merged (#496, #506) and deleted from origin, no worktree exists. The lane's live signal is open PR #521 (2026-08-16) — open PRs outrank this board. The weekly routine re-claims on its next tick per CHARTER §5.5. |
| primary-engineer 2026-08-17 (item 1) | `server/mcp/index.ts` + `server/mcp/softPullGate.ts` (F-042: consent gate ran after the cached-pull return; gate ignored consent type) | 2026-08-17 | **shipped** — gate-first made structural (`readCachedSoftPull` requires the authorized consent as a parameter), scoped via `consentCoversPullType`; 9 new tests in `tests/mcpSoftPullGate.test.ts` prove a refusal never reads `credit_pulls`. Bug reintroduced → 5 fail; fixed → all green. |
| frontend-wiring-audit 2026-08-18 | `client/src/pages/borrower/URLAForm.tsx` (`describeUnsavedRows`) + `client/src/pages/lending/PreApproval.tsx` / `preApproval/entryType.ts` + `client/src/pages/profile/Profile.tsx` (`saveEdits`) | 2026-08-18 | **shipped to review** — branch `claude/interesting-goodall-351b8b`, two fixes. (1) The #451 dropped-row disclosure covered the primary borrower only, while `buildPayload()` filters BOTH slices: a co-borrower's half-filled asset was dropped, reported "Everything is safely stored", then erased by the post-save refetch. (2) `/rates/cash-out` sends `?type=cashout`; the funnel matched only `heloc`, so a cash-out inquiry opened preselected as a PURCHASE. Both proven by reverting (2 of 3 URLA cases fail with the exact bug string). Follow-up (founder-requested) #451-pattern sweep added a third fix: `/profile` silently discarded a CLEARED field — under a success toast when other edits saved, under no message at all when it was the only change. Sweep otherwise clean (4 shapes swept mechanically; 0 mutations anywhere with a success toast and no `onError`). Then ticket 5, **founder-directed and outside §6 territory (`shared/schema/**`) — deviation recorded in the report**: `null` is now the intake schema's third wire state (absent = unchanged, value = set, null = clear), so the root cause behind BOTH #451-pattern defects is gone rather than apologised for. No migration needed — the columns were already nullable. Then ticket 6 (also founder-directed, also outside §6): `zodSchemaSemantics` now unwraps `z.preprocess`/`.transform` to reach the object shape, and a new `all-keys-null` probe pins which fields accept null — the exact class that slipped past it. Snapshot delta audited semantically: **0 schemas and 0 probe VALUES changed**, purely additive. Then ticket 7: the funnel now commits a CLEARED answer to the server draft — detected as a **transition** (a field this session saw hold a value), never as a state, because the form starts blank while the draft may be full and 'empty means clear' would null the whole draft on first debounce. Bundle baseline +47 bytes, justified (one shared catalog beats a duplicated list). Gates green: `check` 0 · node 2795 · client 563 · bundle at baseline · querykeys/tokens OK · `detectTriggers()` `[]`. **Adjacency handled, not raced:** refactor-radar's #530/#532 touch `URLAForm.tsx` ~200 lines away (`STEPS`/`StepContext`); disjoint hunks, whoever merges second takes both. `buildPayload`/`buildSectionsPayload`/`STEPS`/the hydration effect untouched per URLA_FORM_REFACTOR_TRAP. |
| primary-engineer 2026-08-17 (item 3) | `client/src/pages/borrower/CreditConsent.tsx` (ux-20: hard-inquiry fact invisible at the ask) | 2026-08-17 | **shipped** — callout + checkbox label + fine print now name the hard inquiry, mirroring the ratified disclosure item 2; colocated test proves the facts render even with the disclosure document seeded empty. 4 red pre-fix → 543/543 green. |
| primary-engineer 2026-08-17 (item 2, assist) | PR #503 `scripts/bundle-size-baseline.json` (branch `chore/bundle-size-guard`) | 2026-08-17 | **shipped** — CHARTER §5 rung-1 assist on the queue's only red PR: baseline froze at 521,319 on 08-12 while main's merged client work grew the eager entry to a measured 522,148 (+829 raw, +0.16%); branch itself adds zero client bytes (`git diff origin/main...HEAD -- client/` empty). Re-baselined to the measured tip per the guard's own procedure, attribution in the PR comment. |
| refactor-radar 2026-08-17 | `client/src/pages/calculators/AmortizationCalculator.tsx` (RR-015) | 2026-08-17 | **shipped to review** — [#528](https://github.com/barakatammre84/Homiquity/pull/528), the run's one PR. Pure amortization math extracted to `client/src/lib/amortizationEstimate.ts` + colocated characterization tests; page 519→378 lines, behavior-preserving (moved block `diff`-proven byte-identical). **Not merged — owner's call.** Note for peers: `gh pr create` was unusable during a GitHub GraphQL outage that day; the PR was opened over REST (`gh api …/pulls -X POST`), which stayed up. |
| refactor-radar 2026-08-17 (owner-directed) | `client/src/pages/borrower/URLAForm.tsx` (RR-005) | 2026-08-17 | **extraction refused, prerequisite shipped** — [#530](https://github.com/barakatammre84/Homiquity/pull/530). The RR-005 extraction is refuted by 3 adversarial reviews (`handbook/URLA_FORM_REFACTOR_TRAP.md`), re-verified against the current 750-line file. Shipped only the union-narrowing of `STEPS[].id` that the doc names as its prerequisite. **Peers: do not re-attempt the extraction** — it is `blocked-human` in the radar ledger. |
| lender-delivery-gate 2026-08-12 | `tests/mismoXsdValidation.test.ts` (worktree `wt-lender`, branch `routine/lender-gate-2026-08-12`) | 2026-08-12 | **shipped** — eight XSD cases early-`return`ed without `xmllint`, which vitest reports as PASSED; converted to `it.skipIf` so absence is visible. Proven both ways: on `adaa826` with no xmllint the suite reports **19 passed**; on the branch, **8 skipped**. Claim was recorded at commit time, not before the edit — a §5.4 deviation, noted in the report. |
| frontend-wiring-audit 2026-08-12 | the remaining 71 singleton-`queryClient` importers | 2026-08-12 | **shipped** — #504, merged and verified in prod via `/api/health`. The migration is now COMPLETE: `client/src/lib/logout.ts` is the only module-singleton consumer left and carries a comment explaining why it must stay (plain async fn, not a render; and `clear()` on the app's own cache is exactly what it wants). |
| frontend-wiring-audit 2026-08-12 | the 12 `client/src` components importing the singleton `queryClient` **with** a `.test.tsx` sibling | 2026-08-12 | **shipped** — migrated to `useQueryClient()` on `claude/frontend-standardization-2` (`384ab1a`). The other **72** singleton importers are untouched and unclaimed; take them in tested-first batches. Note `TestLogin.test.tsx` had been rendering with no `QueryClientProvider` at all — only the singleton made that pass. |
| refactor-radar 2026-08-08 | `client/src/components/ScenarioSimulatorDialog.tsx` (RR-004) | 2026-08-12 | **abandoned** — run crashed mid-flight and left its worktree behind, hard-blocking every later radar run (`SKILL.md` Phase 0.4). Work snapshotted to `wip/radar-2026-08-08-scenario-simulator-abandoned` (do not merge; superseded by merged PR #467), worktree removed, RR-004 returned to `open`. |

## Known shared-file hazards

Several routines legitimately touch these. Expect conflicts and resolve **additively** rather than
taking one side wholesale:

- **`vitest.config.ts`** — the node-lane `include:` array; more than one routine adds test files.
- **`knowledge-base/README.md`** — the doc index; keep both entries, in date order.
- **`tests/__snapshots__/zod-schema-semantics.json`** — re-recorded by any schema change. **Never
  take one side wholesale**: re-record after merging and re-read every delta, because the snapshot
  is what tells you a data-admission rule changed.

There is deliberately **no "observed in flight" table** here. One existed on the old board and was
stale within hours — every row it listed had merged or moved by the next day. Open PRs are the live
answer to "who else is working", and they are one API call away; a hand-copied snapshot of them is
a claim about the past wearing the clothes of the present.
