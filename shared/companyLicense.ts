/**
 * Small, side-effect-free licensing identity for build-time gates.
 *
 * Keep the NMLS identifier here so the public client can decide whether the
 * commercial funnel is available without pulling the full company identity,
 * licensed-state maps, and disclosure helpers into the eager entry bundle.
 */
export const COMPANY_NMLS_ID: string = "427468";

